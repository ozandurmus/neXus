# Synthetic placeholder — DEV.2.2. Replace with the real MDS host key via
# `ssh-keyscan -H <mds-hostname-or-ip> >> known_hosts`, then verify the
# fingerprint out-of-band. This example line uses an RFC 5737 documentation
# address and is not a real device identity.
192.0.2.10 ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIExampleExampleExampleExampleExampleExampleA
