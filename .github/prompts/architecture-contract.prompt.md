---
description: "Create an implementation-ready SecurityExpert architecture contract"
---

Use the active SESSION START scope. Work in `ARCHITECTURE` movement.

Read only the source/tests needed to establish the dependency graph. Do not
modify code or run network collection.

Produce an implementation-ready contract containing: problem, verified current
behavior, architecture decision, must change, must preserve, security/network
invariants, affected components, tests, real-environment acceptance, rollback,
out of scope, open questions and recommended implementation order.

If the task touches a new device command, apply the network-device command gate.
End with the recommended implementation reasoning level.
