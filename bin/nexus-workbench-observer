#!/usr/bin/env python3
"""Release entrypoint: ship bin/ and scripts/workbench_companion.py together."""

from pathlib import Path
import runpy

runpy.run_path(str(Path(__file__).resolve().parents[1] / "scripts" / "workbench_companion.py"),
               run_name="__main__")
