# 0.6.6A — CP/PAN Parser Correctness Hardening

## Summary

Bounded parser correctness hardening: VSX ifconfig normalization stores canonical network CIDR in interfaces[].ips[].network (host IP kept in .ip), and PAN route classification gives 0.0.0.0/0 precedence over static/connected flags. The two strict characterization xfails became passing regressions; no collector, network, scheduler, concurrency, CAS, storage or UI change.
