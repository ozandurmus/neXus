# NXS-LOCAL-0332 — Check Point discovery timeout (180s) and SSH exec transport stream hardening

## Summary

Raised the Check Point discovery exec timeout to cover multi-domain CMA query times and hardened SshExecTransport against stdin/stderr pipe stalls.
