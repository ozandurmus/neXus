# ui2_d1_option_a_taxonomy_step_kind — UI2 D1 Option A -- apply taxonomy class and restore_push step kind

## Summary

Applied CLASS_1B_CONTROLLED_RESTORE_WRITE at level 1.5 and the C4 restore_push step kind, preserving sftp_put refusal and separating static signability from runtime admission. No C2/C7, Java/UI, device, push, PR, or merge action was performed.

## Evidence

Frozen Option A amendment contract; relay NXS-LOCAL-0064 sequences 1-3; targeted architecture tests passed; baseline-aware repository privacy gate against origin/main reported 0 new findings.

## Risks forward

Runtime restore remains disabled until the separately authorized C7/C2 admission amendments and later test/Java/UI slices land; VSX-context and ClusterXL-member restore remain unsupported.
