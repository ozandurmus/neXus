# ui2_b0_c7_backup_artefact_restore_engine_contract — UI2 B0/C7 -- backup, artefact and restore engine contract (DRAFT, for Product Owner freeze)

## Summary

New docs/design/UI2_0_C7_BACKUP_ARTEFACT_RESTORE_ENGINE_CONTRACT.md: the backup profile model restated field-for-field against C1/C2/C4's real schema (BackupRun/StepResult are C2 jobs/job_step_attempt rows, not new tables; an APPROVED profile version compiles into a C4 capability_registry row reusing C4's cp_gaia_backup_local worked example verbatim); the artefact store/manifest/validation/retention model carrying forward BACKUP_RECOVERY_CONTRACTS.md as a Flyway-owned backup_artefact table, with the frozen restore:null reservation re-expressed relationally (a separate restore_run table referencing the artefact, never a column on it); key custody (recovery_key_custody.py's envelope model inherited via C1 section 6.3, plus a concrete key-rotation procedure that re-wraps the DEK only, never re-encrypts the artefact); restore execution -- plan compilation, a six-check precondition battery, execution via C4's step model, re-collection-based result verification, and a restore-specific OUTCOME_UNKNOWN behaviour naming three possible device states and the out-of-band human verification required before any retry; the approval model (routine backup: D7 role + reason; restore: a non-reusable per-operation restore_approval record, requester != approver); the device/version support matrix filled in for CP Gaia RB.3b and PAN RB.2, both landing declared_supported=false on their actual inherited evidence; 14 acceptance criteria including a restore-failure scenario. Reports rather than silently resolving two load-bearing open items (restore's write does not yet fit any utils/action_taxonomy.py class; C4's closed step-kind set has no device-push/apply step kind), each named for a taxonomy/C4-successor movement -- action_taxonomy.py is not edited. Documentation-only; no ui2/ source, no Line-1 code change, no device contact.

## Evidence

Relay-authorized movement (.nexus/approved_task.json SESSION_START, content-hash-verified against relay/NXS-LOCAL-0057-ui2-b0-c7-backup-artefact-restore-engine.json; the relay file's own single SESSION_START entry re-read immediately before opening the PR, no RELAY_CORRECTION/RELAY_DECISION appended). Written under UI2_0_BASELINE_CONTRACT.md (FROZEN 2026-09-09) -- RESTORE-IN-RELEASE-1 (D-7), APPROVAL-MODEL (D-2c), RAW-RETENTION (D-2d) -- and UI2_0_DEVELOPMENT_WORKFLOW.md §3.5, against UI2_0_C1_PLATFORM_SCHEMA_CONTRACT.md, UI2_0_C2_JOB_EXECUTION_CONTRACT.md, UI2_0_C3_IDENTITY_SESSIONS_RBAC_CONTRACT.md and UI2_0_C4_CAPABILITY_REGISTRY_GATE_RESOLUTION_CONTRACT.md (all four MERGED, same freeze slice) as the schema targets. Read in full before writing: UI2_0_BASELINE_CONTRACT.md, UI2_0_DEVELOPMENT_WORKFLOW.md, BACKUP_RECOVERY_CONTRACTS.md (all sections), BACKUP_AND_RECOVERY_ARCHITECTURE.md (all sections), UI2_0_C1/C2/C3/C4's relevant sections in full, UI2_0_ARCHITECTURE_DESIGN.md §6, D4_BACKUP_CREDENTIAL_IDENTITY_DECISION.md, RECOVERY_OPERATIONAL_WRITE_LEDGER.md, utils/recovery_key_custody.py, utils/action_taxonomy.py. Self-checked against AC-1..AC-12 in the document's own §1/§2/§3/§4/§5/§6/§7/§8/§9. No code changed. This movement's PR was rebased onto origin/main after the concurrent C5 (amendments bundle) movement merged (PR #169) -- C5's own action_taxonomy.py edit is a docstring/comment-only UI-OPERATIONAL-RUN-NOW clarification with no new ActionClass member and CLASS_1_RECOVERY_WRITE's own docstring/fields unchanged, confirmed by reading its diff directly, so this document's §9.1 finding (no class names a restore write) stays accurate and unedited. Full one-shot regression run in the foreground (this worktree has no own .venv; the sibling checkout's validated interpreter at <redacted-local-root>/neXus/.venv/bin/python was used directly, no bootstrap performed) with an explicit long timeout and awaited to actual completion: `python -m pytest -q -n auto --dist worksteal`, 169.70s -- 3211 passed, 27 skipped, 5 failed on the first run (two were this movement's own project-state checkpoint lag, green after the checkpoint edits); a clean re-run after checkpoint edits and again after merging C5 gave 3214 passed, 27 skipped, 2 failed -- the same pre-existing, unrelated DLP-token collisions in project/build_history.json's own prose that op0b_s7_s6_test_order_isolation's baseline (CURRENT_STATE.md) already records as pre-existing on origin/main (tests/test_dev_0_5b_auth_consumer_canonical_config.py's repository-text DLP-collision scan), confirmed unrelated by a stash-and-compare against the pre-change tree. git diff --check clean. Repository privacy gate: FAIL/6 findings, all 6 confirmed pre-existing/unrelated by the same stash-and-compare method.

## Risks forward

The document is DRAFT -- FOR PRODUCT OWNER FREEZE, not yet implementation authority. Two load-bearing gaps block restore's own device execution (never its specification): utils/action_taxonomy.py has no class that names a restore write (CLASS_1_RECOVERY_WRITE's docstring scopes it to backup creation/artefact cleanup only -- unchanged by the concurrent C5 movement's own action_taxonomy.py edit), and C4's closed step-kind set has no device-push/apply step kind (sftp_put is refused unconditionally at current maturity) -- both named as open items for a taxonomy/C4-successor movement, never fixed in this document per its own scope exclusion. A documentation inconsistency is flagged, not corrected: UI2_0_DEVELOPMENT_WORKFLOW.md §4 records RB.2 as REAL_ENV_VALIDATED while BACKUP_AND_RECOVERY_ARCHITECTURE.md §12 explicitly states real-environment validation is owed; this document's own device/version matrix follows the more detailed, conservative source. Off-host key custody, version-mismatch restore override, the retention-deletion typed-intent's exact schema, and multi-member consistency-group restore are each named as open items, not resolved.
