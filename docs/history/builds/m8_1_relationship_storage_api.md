# m8_1_relationship_storage_api — M8.1 -- Device-identity-relationship storage and typed read/write API

## Summary

Additive M4 schema-version-2 migration adding device_identity_relationships (STRICT, closed vocabularies, partial-unique ACTIVE index) plus utils/device_identity_relationships.py's typed read/write API implementing the NEW/SUPERSEDED/AMBIGUOUS_IDENTITY write invariants and a fail-closed read, exactly per the frozen M8 contract §5/§6/§8. No producer, no console consumer, no device contact, no PCP.1 change.

## Evidence

Read docs/history/phase/M8_FIRST_CONTACT_TRUST_AND_IDENTITY_EVIDENCE_PRODUCER_ARCHITECTURE.md (FROZEN) sections 5/6/8/9, utils/control_plane_store.py, tests/test_m4_control_plane_metadata_store.py and utils/device_registry.py (DeviceRecord.updated_at field name) before implementing. Added migration 2 (device_identity_relationships table + ux_device_identity_relationships_one_active_per_triple partial unique index) to utils/control_plane_store.py, bumped SUPPORTED_SCHEMA_VERSION to 2. Added utils/device_identity_relationships.py: closed vocabularies (VENDOR_NAMESPACES, MAPPING_SCOPES, PROOF_TYPES, PROOF_SOURCES, RELATIONSHIP_STATES, INVALIDATION_REASONS), DeviceIdentityRelationship dataclass, record_first_contact_proof (transactional NEW/SUPERSEDED/AMBIGUOUS_IDENTITY write, no parameter for an unproven/identity_mapping_proven=0 row -- §3 point 4 enforced by the API's absence of such a path), get_active_relationship (fail-closed read, never returns INVALIDATED/SUPERSEDED as ACTIVE, defends against an unexpected multi-row read even though the unique index makes it structurally unreachable). Extended tests/test_m4_control_plane_metadata_store.py: fixed the migration-count-sensitive tests that assumed exactly one migration (test_creation_applies_the_initial_schema_version, test_a_valid_shorter_prefix_is_accepted_and_completed and its sibling monkeypatch-based prefix/gap/out-of-order tests, bumped their synthetic extra migration from version 2 to 3 to avoid colliding with the new real migration 2); extended test_schema_owns_no_forbidden_concept's fragment list with 'serial'/'host_key'/'fingerprint' per §8 AC6. Added tests/test_m8_1_device_identity_relationships.py (31 tests): schema shape/STRICT/closed-vocabulary CHECKs, the structural one-ACTIVE-per-triple index, NEW/SUPERSEDED/AMBIGUOUS_IDENTITY transitions (including no-tie-break-after-ambiguity), read-side None/never-returns-invalidated/fail-closed-on-multiplicity, Python-layer closed-vocabulary and empty-identifier validation, and a structural assertion that the write API has no serial/endpoint/credential-shaped parameter. Targeted run: 101 passed (M4 extended + M8.1 new). Affected run: 388 passed, 0 failed (M4/M8.1/M6/M5/PCP.1/CON.2/test_architecture_convergence, incl. the cross-authority consistency check). Repository privacy gate: PASS, 0 findings (data/logs cleared before the run). No full-regression run performed (risk-based, not required for this bounded additive-schema PR per docs/AI_DEVELOPMENT_PROTOCOL.md 'CI validation policy'). Branch build/m8-1-relationship-storage-api from origin/main; PR pending, no merge -- awaiting user authorization.

## Risks forward

AUTOMATED_VALIDATED only, by design: this movement has no device contact, no producer and no consumer, so there is nothing to real-environment-validate yet -- that arrives with M8.3/M8.4. Closed vocabularies (vendor_namespace='checkpoint' only, mapping_scope='CLASS_0_CP_CONFIG_TARGET_SELECTION_ONLY' only, proof_type/proof_source single-valued) are intentionally narrow per the frozen contract; a future PAN producer or a second proof type is its own migration/contract amendment, not a parameter to loosen here. CONTRADICTORY_EVIDENCE detection stays deferred per §6/§10, unbuilt in this movement as required.
