# NXS-LOCAL-0343 — Backup and recovery engine hardening per dual external CLI reviews

## Summary

Removed the insecure daemon, added AST secret scrubbing, Gaia available-space parsing and fail-closed status polling. Hardened store-owned tombstoning, last-backup vault protection and the API edge.
