# ui2_b0_c6_capability_extraction_contract — UI2 B0/C6 -- capability extraction contract (DRAFT, for Product Owner freeze)

## Summary

New docs/design/UI2_0_C6_CAPABILITY_EXTRACTION_CONTRACT.md: the extraction procedure as an ordered checklist against C4's exact registry schema, with a mandatory existing-produces_facts pre-check (worked negative example: OP.0 HA readiness consumes cp_gaia_inventory_show_version_ha_state's ha_state fact instead of re-deriving it); the fixture-generation procedure (utils/support_bundle.py::Tokenizer as the exact entry point, SYNTHETIC/DERIVED marking with one example each, a preserved-property checklist with a proof method per property, the mandatory --repository-privacy-check DLP gate); a fully worked, C4-schema-valid FIRST-CAPABILITY spec plus a described fixture set, cross-referencing C4 §6 rather than duplicating it; the corrected ordered extraction inventory naming each row's target C4 gate-registry entries or C1 table; 12 acceptance criteria for B1-5 and the extraction-tooling movement. Documentation-only; no ui2/ source, no Line-1 code change, no device contact, and no actual extraction of a real capability performed.

## Evidence

Relay-authorized movement (.nexus/approved_task.json SESSION_START, content-hash-verified against the Product Owner's own relay entry). Written under UI2_0_BASELINE_CONTRACT.md (FROZEN 2026-09-09) and UI2_0_DEVELOPMENT_WORKFLOW.md §3.1/§3.4/§3.5/§4, against UI2_0_C4_CAPABILITY_REGISTRY_GATE_RESOLUTION_CONTRACT.md §2/§6 (same freeze slice) as the schema target. Read before writing: UI2_0_BASELINE_CONTRACT.md, UI2_0_DEVELOPMENT_WORKFLOW.md §3.1/§3.4/§3.5/§4, UI2_0_C1_PLATFORM_SCHEMA_CONTRACT.md §3/§5, UI2_0_C4_CAPABILITY_REGISTRY_GATE_RESOLUTION_CONTRACT.md §2/§6, PRIVATE_REPLAY_ARCHITECTURE.md (tokenizer path), BACKUP_RECOVERY_CONTRACTS.md §7.3/§7.4/§7.7/§7.8, docs/AI_DEVELOPMENT_PROTOCOL.md (network-device command gate ten fields). Self-checked against AC-1..AC-10 in the document's own §2/§3/§4/§6/§7. No code changed; ran concurrently with C3 (identity/sessions/RBAC, merged PR #167 while this movement was in progress) and freezes together with C4 per FREEZE-SLICING; full one-shot regression run in the foreground with an explicit long timeout and awaited to actual completion (.venv/bin/python -m pytest -q -n auto --dist worksteal, 169.25s): 3214 passed, 27 skipped, 2 failed -- both pre-existing and unrelated (tests/test_dev_0_5b_auth_consumer_canonical_config.py's repository-text DLP-collision scan tripping on the same known credential-substring pattern already committed in relay/NXS-LOCAL-0030-credential-profiles-reference-model.json and in this file's own earlier gov_po_1_local_relay_watch_command entry; confirmed unrelated by stashing this movement's own changes under a uniquely-tagged git stash entry, re-running both tests against the reverted tree with identical results, then restoring). git diff --check clean. Repository privacy gate (.venv/bin/python main.py --repository-privacy-check): FAIL/9 findings, confirmed pre-existing and unrelated by the same stash-and-compare method (identical 9 findings -- data/, data/.support_hmac.key, logs/ runtime artifacts plus already-committed historical prose in project/backlog.json, project/build_history.json, project/roadmap.json and three relay/*.json files -- before and after this movement's changes, none of them touched by this movement's own diff).

## Risks forward

The document is DRAFT -- FOR PRODUCT OWNER FREEZE, not yet implementation authority. It names no real gate_id and authors no gate-registry row -- a gate-authoring step must still produce docs/AI_DEVELOPMENT_PROTOCOL.md-compliant entries for FIRST-CAPABILITY's two commands before B1-6. The tokenizer's default token shape does not by itself preserve a real identifier's length/character class (serial length/shape property) -- flagged as an open design item for the extraction-tooling movement (ui2_b0_extraction_tooling), not resolved here (Line-1 tool code is out of this document's own scope). Several later queue rows (readiness-table consolidation, CE.2 capability granularity, device-enrollment gate-chain applicability) carry open items left to their own future Extract movements, recorded in §7.
