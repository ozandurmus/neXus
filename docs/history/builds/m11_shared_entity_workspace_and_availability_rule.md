# m11_shared_entity_workspace_and_availability_rule — M11 -- D1-driven availability_rule wiring + shared entity workspace context

## Summary

static/navigation_ui.js::navigationResolveSurface is now the sole, explicit, client-side render/not-render gate for the rail and device-tab strip, mirroring utils/capability_state_resolver.py's stage 0 (resolve_union_tag); a cross-product regression test proves D2..D7 (including a CX1/RI-1/RI-2 contradiction case) never change it. The shared entity workspace (navigation contract section 6.3) extends the existing #<module> hash/localStorage route to #<module>[/<entityId>], with Inventory/Configuration/Compliance publishing selections and each module adopting a shared id only when its own known-id set already contains it. Content-level P2/P3 (a D-NAV13 NOT_APPLICABLE device tab) was RE-SCOPED OUT per this movement's own RELAY_NOTE -- it needs a real-device -> logical-entity-type classifier that does not exist and is not this movement's producer to build.

## Evidence

Relay-authorized movement (.nexus/approved_task.json SESSION_START, content-hash-verified against relay/NXS-LOCAL-0022-m11-shared-entity-workspace-availability.json). AC-2 investigation confirmed CURRENT_STATE.md's predecessor claim (M10.1-M10.3 producers/resolver core unwired to any caller) still held at head, and confirmed no shared-entity-workspace mechanism existed anywhere -- Inventory (selectedId), Configuration (configSelectedId) and Compliance (complianceSelectedSubjectId) each maintained independent, unsynced selection state, exactly the duplication navigation contract section 6.1 names. Found and reported via RELAY_NOTE that a literal reading of 'closing predicates P2 and P3' would contradict the frozen contract's own AC-CS-2/AC-CS-25/AC-CS-33 (P2/P3 must never gate render) and that a real content-level P2/P3 case needs a real-device -> EntityType classifier producer that does not exist and was out of this movement's own scope.out (no change to D2/D3/D4 producers) -- re-scoped accordingly rather than silently completing or dropping the obligation; tracked as project/backlog.json id navigation_entity_type_classifier_for_content_level_p2_p3. Also found the shared-workspace mechanism's practical cross-module carry-over is inert for most real devices today because Inventory's and Configuration's device-id schemes do not overlap (pre-existing gap, RELAY_DECISION #11, still open) -- the mechanism itself was still built correctly (safe id-set-membership adoption, never a guessed cross-module match) so a future canonical-id movement can rely on it unchanged. New test file tests/test_m11_navigation_availability_wiring.py: 13 passed, including two real-Chromium Playwright assertions (render-invariance across varying payload content, and the shared-workspace join primitive end-to-end). Full one-shot regression (DEV.TEST.1, .venv at <redacted-local-root>/neXus/.venv): 2976 passed, 25 skipped, 2 failed -- both pre-existing, unrelated DLP-token collisions in project/build_history.json's own prose (same two tests CURRENT_STATE.md's existing baseline already names). Repository privacy gate: 2 pre-existing findings, neither in a file this movement touched. git diff --check clean.
