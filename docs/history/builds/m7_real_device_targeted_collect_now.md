# m7_real_device_targeted_collect_now — M7 -- real device-targeted Collect now (entity_id-substitution wiring)

## Summary

console/registry_targets.py::_identity_resolved now returns the resolved M8.1 collector entity_id (was bool-only) on every M8.4 currency success; a new resolve_registry_target_entities() shares the identical admission/eligibility/identity checks (via a common _resolve_registry_targets_full() helper) with the unchanged resolve_registry_targets() admission gate, returning the device_id -> entity_id mapping instead of None. console/runner.py::_execute's pre-execution re-check now substitutes the resolved entity_id(s) -- never the raw device_id(s) -- into the argv passed to --cp-config-targets. AC-1(c)'s conditional fingerprint-metadata fix was investigated and found already shipped (PR #103, 2026-09-07), before this movement's own SESSION_START was issued; only two stale-comment locations describing that closed gap as current fact were corrected, no functional change to _collect_host or the M8.3 producer.

## Evidence

Relay-authorized movement (.nexus/approved_task.json SESSION_START, content-hash-verified against relay/NXS-LOCAL-0023-m7-real-device-targeted-collect-now.json). AC-1 investigation, reported via RELAY_NOTE before any code change: (a)/(b) confirmed accurate against current code (_identity_resolved returned bool-only; console/runner.py::_build_argv/utils.collection_executor.workflow_argv passed raw device_ids straight through to --cp-config-targets, no translation step anywhere). (c) found STALE: project/backlog.json's m8_evidence_host_key_fingerprint_not_persisted (PR #103, merged 2026-09-07, merge commit 06e51d36a11ed2ecc8dfc4d7581656aa09c5c95d) already added host_key_fingerprint to configuration/checkpoint_config_collector.py::_collect_host's physical-host store.write_text_snapshot(...) extra_metadata, sourced from the same _connect()/utils.cp_ssh_trust fingerprint the M8.4 trust lookup already uses -- exactly the fix AC-1(c) asked for, already shipped before this SESSION_START (2026-09-08 19:22:48Z) was issued. Scope narrowed accordingly: no fingerprint-metadata code change made; only console/registry_targets.py's own comment above the evidence_fingerprint check and tests/test_m8_4_m6_resolver_consumption.py::_fully_resolved's docstring (both still describing the closed gap as current fact) were corrected. AC-2 implemented as summarized above -- console/app.py's admission-time call site (POST /api/jobs) is unchanged, since admission only needs the refusal gate, not the entity_id mapping. New tests/test_m8_4_m6_resolver_consumption.py::TestEntityIdSubstitution (5 tests): resolve_registry_target_entities returns the correct mapping; empty targets returns an empty mapping; an unresolved target still returns a TargetRefusal; a fully-resolved device_id's constructed argv carries the resolved entity_id (not the device_id) after --cp-config-targets end-to-end through the console job engine (main.main patched); a stale relationship (registry_record_revision bumped after proof) still refuses at admission via the unchanged IDENTITY_TRANSLATION_REQUIRED path, no regression, main.main never called. Targeted run (tests/test_m8_4_m6_resolver_consumption.py + tests/test_m6_registry_keyed_job_targets.py + tests/test_con2_console_job_engine.py + tests/test_con1_operator_console_read_only.py, .venv at /Users/OzanDur/Codo/neXus/.venv): 109 passed. Affected sweep (test_m8_3_first_contact_producer.py, test_m8_1_device_identity_relationships.py, test_m8_2_endpoint_specific_trusted_key_lookup.py, test_pcp1_device_registry.py, test_architecture_convergence.py, test_application_package.py): 179 passed. Full one-shot regression, run in the foreground and awaited to actual completion (.venv/bin/python -m pytest -q -n auto --dist worksteal), first pass: 3025 passed, 25 skipped, 4 failed -- all 4 self-inflicted by this movement's own project-state prose, not the code change: this build's own first-draft roadmap.json/build_history.json evidence text spelled out, in plain prose, the literal credential-assignment-shaped substrings tests/test_dev_0_5b_auth_consumer_canonical_config.py's repository-text DLP-collision scan matches, which the scan correctly flagged as a brand-new (non-baselined) finding at this build's own build_history.json evidence line -- a real self-collision, not a pre-existing one, since the hash-keyed baseline (tests/test_gov_po_3_ci_privacy_gate_baseline.py, tests/test_nexus_engineer_tool_gate.py) only recognizes the two already-committed pre-existing occurrences (project/build_history.json's own m11/gov_po_1_local_relay_watch_command records, relay/NXS-LOCAL-0003), not new text at a new line. Fixed in this same PR by rephrasing both files' prose to describe the collision without spelling the matched substrings out verbatim; no functional/code change. Full one-shot regression, re-run after the rephrase, in the foreground and awaited to actual completion, three separate runs: run 1 (before the rephrase) 3025 passed/25 skipped/4 failed -- the two tests/test_gov_po_3_ci_privacy_gate_baseline.py / tests/test_nexus_engineer_tool_gate.py baseline-hash failures plus the two tests/test_dev_0_5b_auth_consumer_canonical_config.py DLP-collision-scan failures, all 4 caused by this build's own first-draft roadmap.json/build_history.json prose spelling out, verbatim, the literal credential-assignment-shaped substrings that scan matches; run 2 (after the rephrase) 3026 passed/25 skipped/3 failed -- the two baseline-hash failures gone, only the two DLP-collision-scan failures plus one test_phase0_6_x_html_render_performance.py::test_profile_output_html_is_identical_regardless_of_profiling flake (passed cleanly re-run in isolation, a parallel-worksteal-only flake, unrelated to this movement's changed files); run 3 (final, reported below) 3027 passed/25 skipped/2 failed. Across all 3 runs the only recurring failures are the two tests/test_dev_0_5b_auth_consumer_canonical_config.py DLP-collision-scan tests, confirmed genuinely pre-existing and unrelated to this movement by temporarily stashing this movement's own changes and re-running just those two tests against the resulting stashed-clean origin/main-based tree, where they fail identically (same already-documented false positive: project/build_history.json's own m11/gov_po_1_local_relay_watch_command records, relay/NXS-LOCAL-0003, relay/NXS-LOCAL-0012). git diff --check clean.

## Risks forward

Real-device end-to-end confirmation of M7 (a device_id submitted to config_refresh_cp against a genuinely live Check Point gateway, proving the constructed argv both carries the real entity_id and that main.py actually contacts and collects from that device) still needs the Product Owner to run the bounded command per AI_START_HERE.md's Real-environment procedure -- proposed, not executed, by this movement. M12 (per-device/per-capability schedules, D5 producer) now has its M7 prerequisite AUTOMATED_VALIDATED and is reachable; still not started. No other job type's target handling, no M6/M8.4 refusal code/condition/ordering, and no M8.3 producer write-gate semantics were touched.
