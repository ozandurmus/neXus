# Roadmap notes

## Architecture review notes

- 2026-08-25 architecture review: event-driven webhook, crypto/PQC readiness, DEPLOY.1 OIDC boundary and contextual TI enrichment evaluated for CP/PAN scope.
- Vendor scope intentionally limited to Check Point and Palo Alto; no additional vendors will be added at current maturity.
- Webhook signal is a read-only collection trigger only; it must never carry collector commands, device credentials or authoritative topology.
- TI enrichment operates on normalized findings only; raw configuration, full topology and real IP ranges must not leave the evidence boundary.
- 2026-08-31 productization review: retain one worker and the static report while moving to the first server. Do not introduce a generic API around main.py, browser-supplied device commands, per-vendor workers, object storage or Kubernetes until a tested operational need appears. Server-only controls are recorded in docs/design/SERVER_PRODUCTIZATION_AND_MODULARIZATION_ARCHITECTURE.md.
- 2026-09-05 Product Control Plane architecture promotion: reconciled the parked product direction against main ff700e38. One genuine contradiction found and isolated -- a single console enrollment-write boundary covering BOTH the manual and candidate-based enrollment intents together (vs OPERATOR_CONSOLE_ARCHITECTURE.md section 4 wording + the exclusions-write DEPLOY.1A precedent) -> open decision pcp_console_registry_write_gate, decided for neither intent; the discovery-is-the-universe and workflow-is-a-whole-plane assumptions are superseded implementation assumptions, not laws. Storage engine, intervals, MIBs, schema names, UI framework and scheduler technology deliberately not frozen.
- 2026-09-05 Product Control Plane FREEZE (bounded, two mechanical corrections only): (1) the registry mutation lock's release is now instance-safe -- an ownership-token equality check embedded in the lock file at acquisition, compared before every unlink, so a releasing process never deletes a different writer's lock instance that a human recreated after removing a believed-stale one; the lock file itself is classified LOCAL-SENSITIVE (CLASS 2) and, like the registry file, never enters the support bundle (AC-5, AC-15 added). (2) section 22's amendment timing corrected: items 1-3 (OPERATOR_CONSOLE_ARCHITECTURE.md, COMPLIANCE_ASSIGNMENT_AND_FRAMEWORKS.md, BACKUP_AND_RECOVERY_ARCHITECTURE.md) applied at this freeze; item 4 (AI_START_HERE.md 'What this is' sentence) moved explicitly to the PCP.1 close scope because it is only true once PCP.1 ships a persistent registry -- writing it now would misstate current capability in the canonical cold-start document. No architecture or product-direction content reopened beyond these two corrections; docs/design/PRODUCT_CONTROL_PLANE_ARCHITECTURE.md status flipped DRAFT -> FROZEN.
