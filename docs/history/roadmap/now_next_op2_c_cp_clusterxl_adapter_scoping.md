# OP2_C_CP_CLUSTERXL_ADAPTER_SCOPING — op2_c_cp_clusterxl_adapter_scoping: OP.2.C - CP ClusterXL vendor adapter scoping

- Promoted 2026-09-05 (OP.2.1b readiness-policy-amendment session): with D-V7b/D-F3/D-F2 no longer blocking the readiness roll-up, the readiness gate is no longer the reason OP.2.C cannot start -- authorization/trust/adapter/change-management are.
- Adapter itself now IMPLEMENTED + unit-tested (checkpoint/clusterxl_capability_adapter.py, tests/test_op2_c_cp_clusterxl_adapter.py): typed capability()/build_plan()/check_precondition()/execute_once()/observe_postcondition() for the two APPROVED_FOR_OP2C primitives (CP-M1 clusterXL_admin down, CP-M1-R clusterXL_admin up), exercised end-to-end through the real ActionCoordinator with fakes only. Wired into no production adapter_resolver -- CLASS 2 stays structurally unreachable (DenyAllAuthorizer unchanged, no taxonomy member).
- CLOSED 2026-09-05 (op2_c1_cp_clusterxl_member_session): the real ClusterXLMemberSession is now implemented (checkpoint/clusterxl_member_session.py::RealClusterXLMemberSession) over the existing per-member MemberSession transport, unit-tested (tests/test_op2_c1_cp_clusterxl_member_session.py), and unwired -- CLASS 2 still structurally unreachable.
- Still blocked on: DEPLOY.1A OIDC boundary + RBAC OPERATE role (authorize() is still unconditional DENY; no PermitAllAuthorizer exists outside tests); cp_production_ssh_host_key_trust_hardening (P0 before CLASS 2); the signed change-management/network-security review (FAILOVER_ENGINE_ARCHITECTURE.md section 10); a real PreflightProvider/EligibilityEvaluator supplying the check_statuses facts (cluster_mode, subject/peer member tokens, recovery_mode) the adapter's contract expects; a protected CLI/console entry point that actually constructs an ActionCoordinator with a real adapter_resolver, using RealClusterXLMemberSession as its session_resolver target.
- DRAFTED 2026-09-05 (op2_c_change_management_review_package_draft): docs/history/phase/OP_2_C_CHANGE_MANAGEMENT_NETWORK_SECURITY_REVIEW.md compiles the change-management/network-security review package -- unsigned. Sign-off (Product Owner + Network Security Lead, both blank) remains EXTERNAL_SIGN_OFF_REQUIRED and is not this build's action. Drafting it does not unblock DEPLOY.1A, SSH trust hardening, or the protected entry point, all still OPEN_RELEASE_GATE.
- Recommended: Sonnet 5, normal for the remaining wiring items (deterministic against a now-frozen adapter contract); Sonnet 5, extended thinking (high) only for a genuine new decision inside the authorization/trust-model gates.
- Detail: docs/history/phase/OP_2_1B_CP_PILOT_READINESS_POLICY_AMENDMENT.md "Exact next step toward OP.2.C"; checkpoint/clusterxl_capability_adapter.py and checkpoint/clusterxl_member_session.py module docstrings.
- CLOSED 2026-09-05 (op2_c_cp_clusterxl_preflight_eligibility_wiring): a real PreflightProvider/EligibilityEvaluator now exists (checkpoint/clusterxl_preflight_provider.py::ClusterXLPreflightProvider/ClusterXLReadinessEligibilityEvaluator), wired to run_cp_preflight + compute_ha_readiness, unit-tested (tests/test_op2_c_cp_clusterxl_real_preflight_wiring.py), and unwired -- CLASS 2 still structurally unreachable. Still blocked on: DEPLOY.1A OIDC boundary + RBAC OPERATE role; cp_production_ssh_host_key_trust_hardening; the signed change-management/network-security review; a protected CLI/console entry point that actually constructs an ActionCoordinator with a real adapter_resolver, using RealClusterXLMemberSession as its session_resolver target and ClusterXLPreflightProvider/ClusterXLReadinessEligibilityEvaluator as its preflight_provider/eligibility_evaluator.
- op2_c_release_gate_dependency_scoping session (2026-09-05, ARCHITECTURE, read-only): classified the four remaining production-reachability gates named in CURRENT_STATE.md's blocker list. DEPLOY.1A OIDC+RBAC and cp_production_ssh_host_key_trust_hardening are both externally blocked -- the latter's own backlog.json target field reads 'production container/pod runtime hardening (post-DEPLOY.1)', i.e. it shares DEPLOY.1's external server-availability blocker rather than being separately closable. The signed change-management/network-security review (FAILOVER_ENGINE_ARCHITECTURE.md section 10) has no drafted artifact anywhere in the repository yet, unlike its sibling per-primitive network-device command gate (OP.2.1, already DRAFTED); its final sign-off is organizational/external (network-security leads), but the review package itself can be drafted now with zero code, zero server, zero device contact -- the one item in this blocker set that is engineering-actionable today. The protected production entry point + live adapter_resolver construction is dependent on DEPLOY.1A and the SSH-trust hardening both landing first (a live adapter_resolver pointed at RealClusterXLMemberSession in a genuinely production context needs both the auth boundary and the hardened transport in place); it stays explicitly out of scope for any build before then -- 'no production ActionCoordinator/adapter resolver/entry point' stays the correct framing, not something this session weakened. Selected smallest next build: op2_c_change_management_review_package_draft (new upcoming row, below) -- drafting only, no sign-off, no code. No stale JSON/CURRENT_STATE.md contradiction found; cp_device_interaction_safety's already-CLOSED status (backlog.json, 2026-08-25) correctly stays outside today's four-gate blocker set.
- Moved from now_next.next to upcoming 2026-09-05 (product_control_plane_architecture_draft session): not a demotion of any completed OP.2 work and not a change to its blockers -- this row stays blocked on DEPLOY.1 (external) exactly as recorded above; it leaves 'next' only because the horizon contract reserves 'next' for one actionable build, and the Product Control Plane's first movement (PCP.1) is actionable on the laptop without the server. OP.2.D's console flow is expected to live on the PCP.4 device/HA tab so a second console is never built (docs/design/PRODUCT_CONTROL_PLANE_ARCHITECTURE.md section 16/20).
