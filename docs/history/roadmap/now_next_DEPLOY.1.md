# DEPLOY.1 — DEPLOY.1: Ubuntu + Docker server migration

- Blocked on server availability (external). Carries the OIDC viewer boundary, evidence egress policy, CP strict host-key R2 and PAN TLS corporate-CA gates.
