# M7_REAL_DEVICE_TARGETED_COLLECT_NOW — m7_real_device_targeted_collect_now: M7 -- real device-targeted Collect now

- IMPLEMENTED 2026-09-08: console/registry_targets.py's _identity_resolved now returns the resolved entity_id (was bool-only) on every M8.4 currency success; new resolve_registry_target_entities() shares the identical admission/eligibility/identity checks via a common _resolve_registry_targets_full() helper and returns the device_id -> entity_id mapping instead of None. console/runner.py::_execute's pre-execution re-check now calls resolve_registry_target_entities and substitutes the resolved entity_id(s), never the raw device_id(s), into the argv _build_argv passes to --cp-config-targets. resolve_registry_targets's own public contract (admission gate, returns TargetRefusal | None) is byte-identical; no refusal code/condition/ordering changed.
- AC-1(c) investigation finding, reported via RELAY_NOTE before implementation: the SESSION_START's framing that CP config evidence carries no host_key_fingerprint field was STALE -- project/backlog.json's m8_evidence_host_key_fingerprint_not_persisted (PR #103, merged 2026-09-07) already closed that gap, before this movement's SESSION_START was issued. No fingerprint-metadata code change was needed; only two stale-comment locations (console/registry_targets.py, tests/test_m8_4_m6_resolver_consumption.py::_fully_resolved) were corrected to stop describing the closed gap as current fact.
- Fixture-based functional proof (AC-3): tests/test_m8_4_m6_resolver_consumption.py::TestEntityIdSubstitution proves a fully-resolved device_id's constructed argv carries the resolved entity_id (not the device_id) after --cp-config-targets, and that a stale/unproven relationship still refuses via the unchanged IDENTITY_TRANSLATION_REQUIRED admission path (no regression). Real-device end-to-end proof still needs the Product Owner to run the actual console job against a real device -- see this build's RELAY_NOTE/SESSION_CLOSE for the proposed bounded command.
- Validation: targeted 109 passed (test_m8_4_m6_resolver_consumption.py + test_m6_registry_keyed_job_targets.py + test_con2_console_job_engine.py + test_con1_operator_console_read_only.py); affected sweep 179 passed (M8.1/M8.2/M8.3, PCP.1, architecture_convergence, application_package). Full one-shot regression (.venv/bin/python -m pytest -q -n auto --dist worksteal, run in the foreground and awaited to actual completion): 3025 passed, 25 skipped, 4 failed -- all 4 self-inflicted by this movement's own project-state prose (not the code change): this build's first roadmap.json/build_history.json draft spelled out, in plain prose, the literal credential-assignment-shaped substrings tests/test_dev_0_5b_auth_consumer_canonical_config.py's repository-text DLP-collision scan matches (see that test file for the precise substrings), which the scan then flagged as a brand-new, non-baselined finding at this build's own build_history.json line; fixed in this same PR by rephrasing this build's own prose to stop spelling those substrings out verbatim, with no functional/code change. Final re-run (foreground, awaited to actual completion): 3027 passed, 25 skipped, 2 failed -- both confirmed pre-existing and unrelated (reproduce identically against a stashed-clean origin/main-based tree) -- see this build's project/build_history.json record for the full 3-run breakdown.
