# deploy1_evidence_egress_policy — DEPLOY.1 â€” Evidence Egress & Sharing Policy

## summary

Explicit policy and enforcement for every path evidence can leave the system: UI export, support bundle, audit log, webhook ingress persistence, TI enrichment query and OIDC claim retention.

## why

As server, event intake and TI integration add new egress paths, a single governed policy prevents accidental exposure of topology, identities or raw configuration.
