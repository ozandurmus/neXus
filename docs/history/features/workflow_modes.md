# workflow_modes — Development Workflow Modes

## summary

Adds render-only, PAN-config, VSX-only, CP-only and full-checkpoint workflows with mixed-cycle provenance.

## why

Keeps development loops fast without presenting partial reused evidence as a fresh full checkpoint.
