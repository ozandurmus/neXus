# operator_console_operational_actions — Operator Console - operational-write actions

## summary

Per-device Check Point Gaia backup triggered from the console, inside the D3 pilot allowlist, behind a preflight + single-use confirmation token + mandatory reason.

## why

The BackBox-parity moment for the CP pilot set, in its smallest safe form: one device per request, three independent gates, no retry, no restore, no scheduling. Contract: docs/history/phase/CON_3_CONSOLE_OPERATIONAL_WRITE_ACTIONS.md.
