# capability_resolution_module_activation — Product Control Plane - capability resolution + module activation

## summary

Per-device capability projection (inventory, configuration, backup, ha_readiness, controlled_operations, telemetry, diagnostics) from established evidence and implemented vendor support; UNSUPPORTED explicit; never authorization.

## why

Modules light up from evidence, not from labels or user claims; capability != readiness != authorization.
