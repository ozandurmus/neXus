# AI_HANDOVER

Overwrite this file at every session close. Prior versions live in git history;
a dated snapshot is also copied to `docs/history/handover/AI_HANDOVER_<date>_<build>.md`
at close time.

If a section does not apply, write `n/a` — do not delete the heading.

---

## 1. Snapshot

- Product baseline: `0.7.2 — Compliance Follow-ups: Password / Banner / Services
  Projection + Framework Filter & Explain UI` — AUTOMATED_VALIDATED (2026-08-29).
  Closes the list deferred from the 0.7.1 contract.
- Engineering baseline: `DEV.1` complete; `DEV.2.1` (non-interactive runtime
  config) — AUTOMATED_VALIDATED.
- Date: 2026-08-29
- Branch: `feature/0-7-2-compliance-followups` (created off `main`).
  **Not committed or pushed** — Git is human-controlled. `main` is unchanged
  (still at `6f5818e`, the 0.7.1b merge). Working tree holds the 0.7.2 diff.
- Test deps installed for **Python 3.12** (`--user`): `pytest`, `pytest-xdist`,
  `lxml`, `paramiko`, `requests`. Bare `py` on this machine resolves to 3.14
  (no deps). `py -V:3.12 <script>` currently mis-parses on this box (exit 103);
  `py -V:3.12 -m pytest` works, or call the 3.12 interpreter directly at
  `%LOCALAPPDATA%\Programs\Python\Python312\python.exe`.
- Full suite: `py -m pytest -q -n auto --dist worksteal` → **483 passed,
  3 skipped, 0 failed** (~34s).
- Repository privacy gate: on a **clean checkout** PASS / 0 — 0.7.2 adds no
  finding. Locally it reports 5, all pre-existing: `data/`, `logs/`,
  `data/.support_hmac.key` (gitignored runtime artifacts from test runs) and a
  raw interpreter path on line 22 of this file's previous version + its archived
  0.7.1b copy (committed in `6f5818e`). This rewrite drops the raw path.

## 2. Recent builds

- **`0.7.2` — Compliance Follow-ups** (this session, on the feature branch;
  contract `docs/history/phase/0_7_2_COMPLIANCE_FOLLOWUPS.md`, §10 impl record).
  All additive; no server; no new device command.
  - **Projection extension** over the *already-stored* config (no new
    collector): new sections `password_policy`, `banner` (presence + coarse
    length bucket only — never the body), `services`.
    - PAN (`configuration/current_config_projection.py`): bounded local-name
      **allowlist extractors** (`_allowlist_section` + `_has_ancestor`);
      `_section_for()` now returns `None` for `/deviceconfig/system/service/*`
      and `login-banner` / `motd` / `ssh-banner` leaves so the generic scalar
      walk no longer double-projects service toggles under Management or exposes
      the banner body under System. `CURRENT_CONFIG_SCHEMA_VERSION → "0.7.2"`.
    - CP (`configuration/checkpoint_config_collector.py`): `PASSWORD_POLICY_SAFE_RE`
      re-admits `set password-controls <knob>` lines that `SECRET_LINE_RE` would
      withhold; `MESSAGE_BODY_RE` collapses `set message … msgvalue <body>`
      before the redacted `sanitized_text` / `safe_set_lines` (which feed the
      shareable bundle). CP artifact `schema_version` deliberately left `"0.6.1B"`.
  - **+6 enrichment controls** (`utils/compliance_catalog.py`
    `CATALOG_VERSION → "0.7.2"`; `utils/compliance_evaluators_ext.py`):
    `password_min_length`, `password_complexity_enabled`,
    `password_history_depth`, `password_lockout_policy`,
    `login_banner_text_present`, `unused_services_disabled`. In
    `subject["extended_controls"]` only; the frozen 0.6.6B ten and
    `rule_count == 10` are untouched. Section absent → `UNKNOWN`;
    present-but-weak → `FINDING`; never an inferred `PASS`.
  - **UI** (`templates/index.html`, `static/app.js`, `static/style.css`):
    framework **filter chips** (CIS / PCI-DSS / BDDK, multi-select union, Clear)
    that filter the control grids + framework-readiness cards + subject grids;
    an inline **Explain** toggle on every control card (rationale + evidence
    fields + framework references; one document-level delegated listener). No
    payload change for the UI parts.
  - Tests: new `tests/test_phase0_7_2_compliance_followups.py` (6). Frozen
    touch-ups: `test_phase0_7_1a` + `test_phase0_7_1` (`CATALOG_VERSION`),
    `test_phase0_6_0a4_3` (`CURRENT_CONFIG_SCHEMA_VERSION`). `test_phase0_6_6b`
    untouched.
- **`0.7.1b`** — file-based per-device control assignment + waivers, +8
  enrichment controls, additive `compliance_overview` + `assignment_policy`,
  Overview card + coverage / framework-readiness band.
  `docs/history/phase/0_7_1_COMPLIANCE_ASSIGNMENT.md`.
- **`0.7.1a`** — the ten controls moved into `utils/compliance_catalog.py`
  verbatim with `severity` / `rationale` / real per-framework membership.
- **`0.7.0`** — crypto posture / crypto-agility / PQC readiness from the stored
  PAN XML → static rule pack, additive payload, one Compliance card.
- Earlier: `0.6.6B` compliance rule pack, `DEV.2.1` non-interactive runtime
  config, `clean_baseline_bootstrap`, Failover Engine design (`OP.x`).

## 3. Next work

**No active build contract is open.** `0.7.2` is AUTOMATED_VALIDATED on its
feature branch and needs a human `main` merge (see §6). The next build needs a
fresh contract, written and put to the user for review first.

**User-chosen direction (2026-08-29):** build a **user-authored compliance check
engine** — a check is *data* (name + evidence source + expected pattern +
verdict), the BackBox/Nipper model — instead of hand-written Python per control.
Design written and reviewed: **`docs/design/COMPLIANCE_CHECK_ENGINE.md`**,
phased CE.1 → CE.2 → CE.3. User picked "both, phased" (whole engine designed
now, CE.1 implemented first).

- **Recommended next contract: `CE.1` (target `0.7.3`)** — data-driven assertion
  engine + fail-closed RuntimeRoot check pack (`data/state/compliance_checks.json`,
  mirrors `utils/control_assignment.py`) over **already-collected** evidence
  (`current_configuration` projections + `unified.json` + `crypto_facts` +
  `alignment`). Fixed operator set, `evidence.steps[]` with `all`/`any`, anchored
  regex behind a length cap + timeout + complexity linter, results into
  `subject["extended_controls"]` (`control_class: "user_check"`) +
  `compliance_overview`; 0.7.2 Explain panel + framework chips reused;
  `advisory`/dry-run check state as the false-positive guard. **No new device
  command, no server.** New files: `utils/compliance_check_engine.py`,
  `utils/compliance_check_pack.py`. Open decisions: design doc §10.
- `CE.2` (curated read-only command-primitive registry) is **blocked** on
  `cp_device_interaction_safety` (P0) + the network-device command gate.
  `CE.3` (UI check editor + signed org packs) is `DEPLOY.1A`-gated.
- Backlog item: `compliance_check_engine`.

Other candidates if not doing CE.1 next:

- **Remaining `0.7.x`** — `framework_mappings`, `evidence_reporting`; a
  point-in-time / trend layer for `compliance_overview` off the config history.
- **`crypto_agility_pqc` follow-ups** — dynamic/signed packs, scoring, live
  `negotiated` layer (needs a server).
- **Standing doable-now** — `immutable_store_permission` (P1 bug),
  `html_render_performance` (P2), `inventory_exclusions_ui` /
  `overview_device_lifecycle_enrichment` (P1 UI).
- **Repo hygiene (small)** — the privacy gate flags `data/` / `logs/` /
  `data/.support_hmac.key` being written into the repo dir by test runs
  (`DEV.0.3C` deferred; backlog `dev_python_env_tooling_friction` adjacent).

**OP.x — Controlled Failover: design done, approval pending.**
`docs/design/FAILOVER_ENGINE_ARCHITECTURE.md`; `roadmap.json` `open_decisions`
holds the 6 review items. OP.0 (read-only HA readiness) and OP.1 (dry-run plan
compiler) need only a reachable test CP + PAN cluster; OP.2 (write) is hard-gated.

## 3b. NOT YET DONE — real-environment / on-hardware validation

Automated tests are green but nothing since the 0.6.1x builds has been exercised
end-to-end. Backlog `on_hardware_real_env_validation` (P0), blocked on the
corporate laptop.

- **Local render check (no devices):** `py -V:3.12 scripts/render_sample.py`
  builds a synthetic `unified.json` and renders `index.html` (path printed).
  Configuration / Compliance / Discovery show their correct "no evidence"
  states. 0.7.2 verified 2026-08-29: exit 0, 0 placeholders, the new
  `complianceFrameworkFilter` / chip / explain-toggle markup present in the
  inlined output.
- **Real collection run:** needs reachability to an MDS / Panorama plus
  credentials — deferred to the server. The 0.7.2 `password_policy` / `banner` /
  `services` sections and their 6 controls only populate once a real
  configuration collection has run; on a laptop render they show "no evidence".
- **DEPLOY.1** (`now_next.next`): server migration, gated on hardware arrival.

## 4. Open risks / debt carried forward

- CP device-interaction-safety audit remains P0 (blocks any scheduling /
  concurrency increase).
- `_realenv_*.py` / `_write_r0x_policy.py` stay at repo root (imported by
  `tests/` and validation runbooks).
- `.py` source comments still cite `PHASE0_*.md` by bare filename.
- `scripts/pytest_one_shot.ps1` calls `py`; on this machine that resolves to
  3.14 without deps (backlog `dev_python_env_tooling_friction`).
- The CAS / support-key path writes `data/` and `logs/` into the repo dir during
  a test run (`DEV.0.3C` deferred). Gitignored; the privacy gate flags them.
- **0.7.2:** the CP `show configuration` sanitized artifact previously carried
  the banner body verbatim; 0.7.2 now redacts it (`MESSAGE_BODY_RE`). The PAN
  `login-banner` body was previously projected in full under the `system`
  section; 0.7.2 removes it (presence-only `banner` section). Both are privacy
  improvements — flag on the next real-env run that no downstream consumer
  depended on the banner text.
- 0.7.2 enrichment evaluators are conservative — on real sparse projections
  several will read `UNKNOWN` until the `password_policy` / `banner` / `services`
  sections are actually populated by a collection run. By design (never an
  inferred PASS).
- CP artifact `schema_version` stays `"0.6.1B"` though the CP projection changed
  in 0.7.2 (new sections). Deliberate — many CP tests pin it and it is a
  collector-artifact schema, distinct from the PAN `CURRENT_CONFIG_SCHEMA_VERSION`.

## 5. Exact next action

1. Human review + `main` merge of `feature/0-7-2-compliance-followups` (§6).
2. Then: start a fresh chat, pick a candidate from §3, write and review a
   contract before implementing.

## 6. main merge decision + Git dispatch

Recommendation: **approved for `main`** once a human has reviewed the diff.
Evidence: 483 passed / 3 skipped / 0 failed; render exit 0 / 0 placeholders;
privacy gate adds no finding (5 pre-existing, documented). Additive only; no new
device command; no collector / network / CAS / scheduler / assignment-engine
change; frozen 0.6.6B pack and `subject["controls"]` unchanged.

Non-interactive dispatch for the recommended path (run by a human):

```
git add -A
git commit -m "feat(compliance): 0.7.2 — password/banner/services projection + framework filter & explain UI"
git push -u origin feature/0-7-2-compliance-followups
gh pr create --base main --head feature/0-7-2-compliance-followups \
  --title "0.7.2 — Compliance Follow-ups: password/banner/services projection + framework filter & explain UI" \
  --body "Contract: docs/history/phase/0_7_2_COMPLIANCE_FOLLOWUPS.md (§10 impl record). Additive; no new collector/command. pytest 483p/3s/0f; render exit 0; privacy gate adds no finding."
```

## 7. Next movement / model

- Next movement: `ARCHITECTURE` (new contract for the chosen candidate), then
  `IMPLEMENTATION`.
- Reasoning: high for a new-feature contract (especially anything touching the
  config-history / point-in-time layer or pack signing); normal for
  deterministic implementation, tests, docs.

## 8. Continue or fresh chat

**Start a fresh chat.** 0.7.2 is complete pending the human merge; the next
build is a different objective and needs its own contract.

## 9. main.py / UI effect

After a normal run *with* configuration evidence: the Compliance module shows
6 extra control cards in the enrichment (`extended_controls`) area, three
framework filter chips above the fleet view, and an "Explain" toggle on every
control card; the Overview compliance summary control totals rise by 6. With
*no* configuration evidence (e.g. `scripts/render_sample.py`): the new controls
render in the existing "no evidence" state and the chips operate on the empty
list — no error. No change to Network Inventory, Configuration, Discovery, or
Project Plan.
