# AI_HANDOVER

Overwrite this file at every session close. Prior versions live in git history;
a dated snapshot is also copied to `docs/history/handover/AI_HANDOVER_<date>_<build>.md`
at close time.

If a section does not apply, write `n/a` — do not delete the heading.

---

## 1. Snapshot

- Product baseline: `0.7.6a — Render harness + uitest topology matrix` —
  AUTOMATED_VALIDATED (2026-08-30). Prior 0.7.x: `0.7.6` (harness), `0.7.5`
  (trend layer), `0.7.4` (framework requirements); hotfix `0.7.4a`
  REAL_ENV_VALIDATED.
- Engineering baseline: `DEV.1` complete; `DEV.2.1` — AUTOMATED_VALIDATED.
- Date: 2026-08-30.
- **`origin/main` is at `671fd6c`. Working tree clean, nothing pending.**
  Everything from this session is merged + pushed (see §2).
- Full suite: `py -m pytest -q -n auto --dist worksteal` → **551 passed,
  3 skipped, 0 failed** (~35s). Run the 3.12 interpreter directly at
  `%LOCALAPPDATA%\Programs\Python\Python312\python.exe`
  (`py -V:3.12 <script>` mis-parses on this box, exit 103; `py -V:3.12 -m pytest`
  is fine). Bare `py` → 3.14 without test deps.
- Repository privacy gate: **PASS / 0 on a clean checkout**. A test run writes
  gitignored `data/` + `logs/` into the repo dir — delete them before the gate.
- **HTML render harness (0.7.6):** `py -V:3.12 scripts/render_uitest.py --out D`
  then `bun tools/render-harness/check-render.mjs D/output/index.html`.
  `tests/test_html_render_harness.py` wires both; the `bun` half `skipif`s when
  `bun` (`~/.bun/bin/bun`) or `tools/render-harness/node_modules/` is absent —
  `bun install` in `tools/render-harness/` once. **Mandatory for any
  `templates/` / `app.js` / `style.css` / payload-builder change** (`AGENTS.md`
  close checklist + `docs/AI_DEVELOPMENT_PROTOCOL.md`), and extend
  `tests/fixtures/uitest/` per its README growth rule.
- **GitHub tooling:** `gh` CLI installed (`C:\Program Files\GitHub CLI\gh.exe`,
  v2.98.0), authed as `ozandurmus`, scopes `gist, read:org, repo, workflow`.
  `gh auth setup-git` done → `git push` / `fetch` on
  `https://github.com/ozandurmus/neXus.git` need no prompt. Corporate Git
  push/merge stays human-initiated (`CURRENT_STATE.md` standing priority 4);
  `gh pr create` / `gh pr merge` is a valid alternative under the same go-ahead.
- **Never label a version as four dot-separated numbers** — `_IPV4_RE` in the
  privacy gate flags an `A.B.C.D` label as `PRIVATE_ENDPOINT_LITERAL`. Use a
  letter suffix (`0.7.4a`).

## 2. Recent builds (this session) — all LANDED on `origin/main`

Detail is in `project/build_history.json`; phase docs under
`docs/history/phase/`. Newest first:

- **`0.7.6a`** (`671fd6c`) — `tests/fixtures/uitest/` expanded to a full topology
  matrix: CP standalone gateway / ClusterXL (active+standby) / VSX host + virtual
  systems / VSX cluster + shared virtual system / UNAVAILABLE gateway; PAN single
  / HA pair / multi-vsys / multi-vsys HA. Plus cluster-member interface+route
  divergence, `live`/`last_known_good`/`no_data` inventory, the full alignment
  class set, `same`/`changed`/`first` history, crypto `PASS`/`FINDING`/`UNKNOWN`,
  enforced + advisory + WAIVED compliance. New `test_all_topologies_present`.
  `0_7_6_RENDER_HARNESS.md` §4.
- **`0.7.6`** (`bf9c605`) — automated HTML render harness: committed
  `tests/fixtures/uitest/` bundle, `scripts/render_uitest.py`,
  `tools/render-harness/check-render.mjs` (bun + `happy-dom` — parse + execute
  the inline `<script>`, click every nav module + tab, assert panels switch with
  no console errors), `tests/test_html_render_harness.py`. Governance:
  `AI_DEVELOPMENT_PROTOCOL.md` + `AGENTS.md`. No `main.py` change.
  `0_7_6_RENDER_HARNESS.md`.
- **`0.7.5`** (`8aa0805`) — compliance trend layer. `utils/compliance_history.py`
  (append-only ledger `data/state/compliance_history.json`, fail-safe read, cap
  200, aggregates + ISO dates only). `build_compliance_posture(..., history=)`;
  `compliance_overview.history[]` + `.trend` (additive). `html_export` reads
  every render, writes only on a full checkpoint
  (`run_html_export(record_checkpoint=True)` from `main.py`). `app.js` sparkline
  + "±N pts since <date>" chip. `COMPLIANCE_SCHEMA_VERSION` unchanged.
  `0_7_5_COMPLIANCE_TREND.md`.
- **`0.7.4a`** (`7d991a3` + real-env doc `0c89880`) — P0 render hotfix.
  `html_export` chained `str.replace()` per placeholder; a `project/*.json` note
  contains the literal `__CRYPTO_JSON_PLACEHOLDER__`, so once `projectPlanData`
  was embedded the later replace spliced the crypto JSON into that string
  literal → `SyntaxError` → the whole inline `<script>` dead → every nav button
  dead. Fix: `_fill_template` — one `re` alternation pass, inserted content never
  re-scanned. **REAL_ENV_VALIDATED** (product owner, corporate laptop).
  `0_7_4A_HTML_EXPORT_RENDER_HOTFIX.md`.
- **CE.1 fast-follow #2** (`5b5e893`) — wired `unified.interfaces` /
  `unified.routes` into the check engine: `build_compliance_posture(...,
  unified_inventory=)`, vendor-scoped normalised-identity join, load-restricted
  to presence/count ops, count-only `observed` (no network identity in the
  payload). `0_7_3_COMPLIANCE_CHECK_ENGINE.md` §12.

## 3. Next work

**No active build contract is open.** A new build needs a fresh contract, put to
the user for review first.

- **`compliance_overview.trend` retro-fill / reconstruction** (optional) — a
  TRACE-plane build recomputing past posture from the content-addressed config
  history so the trend has depth before the first two live checkpoints. `0.7.5`
  deliberately did not (append-only ledger, no backfill). `Sonnet 5, extended
  thinking` for the contract.
- **CE.2** (`compliance_check_engine_primitives`) — curated read-only
  command-primitive registry, opt-in `--compliance-probe`. **Blocked on
  `cp_device_interaction_safety` (P0) + the network-device command gate.**
- **CE.3** / **CE.4** — `DEPLOY.1A` / OP.2-gated.
- **Standing doable-now:** `immutable_store_permission` (P1 bug),
  `html_render_performance` (P2), `inventory_exclusions_ui` /
  `overview_device_lifecycle_enrichment` (P1 UI).
- **OP.x — Controlled Failover:** design done, approval pending
  (`docs/design/FAILOVER_ENGINE_ARCHITECTURE.md`; `roadmap.json` `open_decisions`).

## 3b. NOT YET DONE — real-environment / on-hardware validation

Automated tests green; the render harness now covers report load + navigation
without a human. Still owed on real hardware (`on_hardware_real_env_validation`,
P0, laptop/server-blocked):

- **`0.7.5`**: the trend sparkline/chip appears only after a *second* real full
  `py .\main.py` checkpoint (ledger starts empty, no backfill). No action needed
  — it fills over the user's next runs.
- Real collection run: needs an MDS / Panorama + credentials — deferred to the
  server. Requirement coverage / user-check outcomes only become PASS/FINDING
  once a real configuration collection has run.

## 4. Open risks / debt carried forward

- CP device-interaction-safety audit remains P0 — hard prerequisite for CE.2.
- `unified.interfaces` / `unified.routes` join is an exact normalised-identity
  match on the config-UI device name. If a real run's config-subject name and
  `unified.json` `device` string diverge, the namespace stays `UNKNOWN`
  (fail-closed, not wrong). VSX cluster-name / per-VSID scoping not attempted —
  confirm the join lands on the first real-env run before authoring
  interface/route packs.
- `0.7.5` trend ledger: aggregates only, no backfill. A catalog / control-set
  version change between runs is recorded per-record but not annotated on the
  trend line — a jump may be a rule change, not a posture change.
- The `uitest` fixture is authored at the payload layer. If a builder
  (`build_configuration_ui_payload` etc.) changes its output shape, the fixture
  must be regenerated to match (growth rule) — the harness can't catch a fixture
  that has drifted from the real builder.
- The regex safety linter (`_REDOS_RE` + quantifier count + `.*.*`) is
  best-effort; the eval-time timeout is the real backstop.
- CP `show configuration` sanitized artifact redacts the banner body (0.7.2
  `MESSAGE_BODY_RE`); PAN `login-banner` body is no longer projected. Confirm on
  the next real-env run that no downstream consumer needed the banner text.
- `0.7.4` framework catalog requirement lists are hand-authored — a new control
  needs its `frameworks[].reference` added as (or matched to) a requirement or
  it surfaces in `by_framework[name].unmapped_control_refs`.
- The CAS / support-key path writes `data/` + `logs/` into the repo dir during a
  test run (`DEV.0.3C` deferred). Gitignored; delete before the privacy gate.
- `scripts/pytest_one_shot.ps1` calls `py` → 3.14 without deps
  (`dev_python_env_tooling_friction`).

## 5. Exact next action

**Fresh chat.** Cold-start via `AI_START_HERE.md` → this file → `CURRENT_STATE.md`
→ `project/roadmap.json` + `project/backlog.json`. Pick one §3 objective, write a
contract for user review **before** implementing. Run the render harness (§1) for
any UI/payload change.

## 6. main merge decision + Git dispatch

- **Nothing outstanding.** `origin/main` at `671fd6c` holds this entire session
  (CE.1 fast-follow #2, `0.7.4a`, `0.7.5`, `0.7.6`, `0.7.6a`).
- Future builds: branch off `main`, commit, `git merge --no-ff` + `git push
  origin main`, or `gh pr create --fill --base main` → `gh pr merge --merge`.
  Human-initiated per standing priority 4.
- `bun.lock` + `package.json` under `tools/render-harness/` are committed;
  `node_modules/` is not. Delete gitignored `data/` + `logs/` before the privacy
  gate.

## 7. Next movement / model

- `IMPLEMENTATION` (**Sonnet 5, normal**) for any standing doable-now item (§3).
  Nothing pending needs Opus.
- A new 0.7.x / 0.8.x design (trend reconstruction, OP.0) wants **Sonnet 5,
  extended thinking** for the contract only, then normal for implementation.

## 8. Continue or fresh chat

**Start a fresh chat.** This session closed five builds; the next objective is
distinct and needs its own contract.

## 9. main.py / UI effect

- **0.7.6 / 0.7.6a:** none — dev/CI tooling + test fixture only. `main.py`
  unchanged.
- **0.7.5:** no visible change until a *second* full `py .\main.py` checkpoint.
  From then the Overview compliance card + the Compliance KPI band show an
  aligned-% sparkline and a "±N pts since <date>" chip. `--render-only` never
  writes the ledger; a corrupt ledger degrades to "no trend", never an error.
  Payload gains `compliance_overview.history[]` + `.trend` (additive).
- **0.7.4a:** the generated `output/index.html` runs its inline `<script>` again
  — module-nav buttons, tab switching and every interactive panel work. A
  re-render of an existing run is enough; no payload/schema change.
- **CE.1 fast-follow #2:** no visible change unless a pack ships an
  `unified.interfaces` / `unified.routes` check — that check now evaluates
  against real merged inventory (was always `UNKNOWN`); its Explain `observed`
  line reads `"N inventory row(s)"` (count only).
- **0.7.4:** each framework readiness card in Compliance shows a
  `version · profile` line, a COVERED/PARTIALLY/UNCOVERED/N-A requirement
  mini-bar, and a `Requirements (N)` expand. Framework-level percentages
  unchanged.
