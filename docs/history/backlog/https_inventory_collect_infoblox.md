# Infoblox Collect: a real https_inventory_collect job (Collect now + 23:00 schedule) reads grid members, each member's interfaces (LAN VIP, MGMT, LAN2, additional IPs) and static routes into inventory runs; member reads leave the confirm and backup paths (backup only backs up)

status: in_progress · target: docs/design/VENDOR_BACKUP_CONTRACTS_2026_09_22.md

2026-09-25 ~03:50: built. https_inventory_collect (InventoryCapabilityIds, registered in DeviceCompositionConfiguration, InventoryCollectService maps infoblox/radware; roles appliance and management_server admitted for HTTPS vendors). Worker: HttpsInventoryJobExecutor + HttpsVendorExecutor.inventory -- Infoblox reads the member list once and records a run with a context per member (LAN1/MGMT/LAN2/additional interfaces, static routes), member facts and names; confirm is identity-only again; the backup path no longer reads members (sink removed). Radware Cyber Controller: the managed device list as names. Tests: worker 294, job-engine 72, service 65 green. Interface/route field shapes are parsed defensively from the WAPI docs; first real run will log field names (counts only) -- PO can run ~/wapi_member_ifaces.sh on HOST-A for a masked measurement.

2026-09-25 02:38: real-environment validated. Collect now on the Grid Manager admitted (202) and completed in 354 ms: 7 contexts (one per member), 17 interfaces (LAN1 x7, MGMT x6, LAN2 x4), 1 static route, 7 member fact rows; the Devices tree lists the members, selecting one shows its interfaces and routes, Routing shows the static route. Two follow-ups shipped the same night: a member-context device shows its first member until one is selected; the inventory re-reads the grid name (GET /grid) so the device keeps the grid's own name (the first run had renamed it to the Grid Master's host name -- corrected by deploy 7 and the next Collect). Measurement record updated.
