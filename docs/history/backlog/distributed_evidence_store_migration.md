# DEV.3.3 - CAS metadata index, run manifests and last-known-good to Postgres

status: automated_validated · target: DEV.3.3

AUTOMATED_VALIDATED 2026-08-31. Contract frozen and implemented: docs/history/phase/DEV3_3_DISTRIBUTED_EVIDENCE_STORE_MIGRATION.md (includes implementation-time amendments A1-A9). utils/evidence_backend.py adds four backends -- config snapshot metadata index, run manifest, last-known-good, scheduler state -- filesystem by default (behavior unchanged) and PostgreSQL when SECURITYEXPERT_EVIDENCE_BACKEND=postgres, independent of DEV.3.2's SECURITYEXPERT_COORDINATOR_BACKEND. CAS payload blobs stay on the volume. E1 resolved by the product owner to full identity fidelity; the Postgres instance is now a documented CLASS 2 asset in PRIVACY_AND_DATA_HANDLING.md. Two substantive findings during implementation: per-entity last-known-good writes are required for the concurrency fix to be real (A1), and CREATE TABLE IF NOT EXISTS needed an advisory lock to survive two containers starting together (A9, caught by the real-subprocess test). Evidence: 17 new tests against a real local PostgreSQL 16; full suite 788 passed / 3 skipped / 2 pre-existing unrelated failures; privacy gate PASS/0. REAL_ENV_VALIDATED/DONE still owed: a multi-container run proving last-known-good state for a fleet split across containers matches a single-container run (server-blocked, DEPLOY.1).
