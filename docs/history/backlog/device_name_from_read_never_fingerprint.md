# Device names: the Check Point presented SSH identity is a host-key fingerprint on this estate and had been stored as the MDS hostname; inventory now reads show hostname (fixed 2026-09-25) -- audit the confirm path for the same and any device row still carrying a fingerprint as its name

status: planned · target: ui2/worker/src/main/java/com/securityexpert/nexus/ui2/worker/confirm/ConfirmJobExecutor.java

2026-09-25 ~12:00: correction to the morning's count. The 68 'empty' devices were the device row's observed_* fields, which were never filled for MDS-discovered Check Point gateways (the pre-rebuild export shows the same 0/64); the Devices screen shows hostname, model and version from the MDS discovery candidate, unchanged and refreshed nightly at 01:00. Nothing was deleted (audit log: no update cleared a hostname). Bulk Collect filled observed facts for 55 of 64 gateways; the rest are 3 unreachable, 2 DRAFT and 4 Quantum Spark. Quantum Spark: neither clish -c nor bare 'show hostname'/'show version all' exist there (measured: 'Bad parameter starting at ...'); no identity read is sent to a Spark any more -- its identity stays with discovery. Gate cp_spark_show_hostname (V73) is kept as history and never issued.
