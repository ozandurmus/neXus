# Behavior-preserving modularization of large frontend and workflow modules

status: done · target: Local development, independent of server arrival

Follow docs/design/SERVER_PRODUCTIZATION_AND_MODULARIZATION_ARCHITECTURE.md. First split static/app.js into responsibility-owned source fragments composed back into the same inline portable report; then extract main.py workflow seams and vendor collector seams. Every extraction is behavior-preserving and protected by render/privacy/full-suite gates. FRONTEND HALF CONTRACT FROZEN 2026-08-31 (Sonnet 5, normal; docs-only, no code change), immediately after and grounded in the same session's frontend_rendering_boundary full read of static/app.js: docs/history/phase/CODEBASE_MODULARIZATION_FRONTEND.md. FRONTEND HALF IMPLEMENTED 2026-09-01 (Sonnet 5, normal): static/app.js removed; its 4,905 lines / 173 top-level functions distributed across the eight D-MOD5 files (app_core, inventory_ui, configuration_ui, compliance_ui, discovery_ui, project_plan_ui, overview_ui, app_bootstrap) with zero code lines lost (verified by line-multiset diff of the rendered report before/after: the only additions are eight file-header comments plus a five-line relocation note). utils/html_export.py concatenates the eight files in fixed dependency order into the same single __SCRIPT_PLACEHOLDER__ fill; new compose_report_script() helper; sixteen source-string UI tests repointed to it. New tests/test_frontend_module_composition.py (AC-3 static dependency-order check + AC-1 completeness). Two contract-audit gaps found and resolved by the ownership rule: currentConfigurationFleet moved to configuration_ui (both it and overview_ui call it; config loads first); switchModule/savedModule kept in app_bootstrap (loads last) with the AC-3 check carrying a reasoned nav-dispatcher carve-out since every feature-module call site is a deferred event handler. Render harness (bun) green on both the uitest and empty-state renders; privacy gate PASS/0 on a clean checkout. Amends the architecture doc's 7-file proposal with an 8th file (overview_ui.js) and two deliberate deviations: no window.SecurityExpert namespace, and app_core.js carries no shared-state bucket. Status stays in_progress: a human interactive real-browser open of the frontend split is a cheap non-blocking follow-up (this sandbox has no display; bun DOM-execution harness stood in). BACKEND HALF CONTRACT FROZEN 2026-09-01 (Sonnet 5, normal; docs-only): docs/history/phase/CODEBASE_MODULARIZATION_BACKEND.md -- a new application/ package (cli.py / services.py / context.py / workflows/{maintenance,recovery,checkpoint}.py) reducing main.py to a thin ~120-line entry, an ApplicationContext dataclass for the ~15 shared locals, main.py re-exporting the seven names 12 test files import (no test rewrite), and the lazy vendor-import boundary turned into a tested invariant (AC-3 static + AC-5 runtime). Scopes the main.py orchestration seam ONLY; the vendor-collector split (configuration/pan, configuration/checkpoint) stays a separate feature-triggered item. BACKEND HALF IMPLEMENTED 2026-09-01 (Sonnet 5, normal): main.py (2,089 lines) reduced to a 47-line thin entry delegating to application.cli.run; the application/ package (cli.py, services.py, context.py, workflows/{maintenance,recovery,checkpoint}.py) matches the frozen ownership table, moved verbatim apart from de-closuring locals into an ApplicationContext. Full suite 896/26/2 (same two pre-existing pollution failures), zero regressions; privacy gate PASS/0; AC-3/AC-5/AC-6 all pass (new tests/test_application_package.py); AC-4 before/after CLI transcript diff over the offline mode matrix is empty except run-scoped noise. One real pre-existing gap the new AC-3 check surfaced (not introduced by this build): main.py's top-level utils.config_storage import was already pulling in lxml transitively on every invocation; closed by making it lazy in storage_analyze()/storage_deduplicate(). Both halves of this backlog item are now DONE; the vendor-collector split (configuration/pan/, configuration/checkpoint/) was scoped out of both and, per the backend contract, opens as its own feature-scoped backlog entry only if/when a bounded feature needs it. The frontend half's human interactive real-browser open remains a cheap, non-blocking follow-up, not a reason to keep this id open. REAL-BROWSER OPEN CLOSED 2026-09-01 (Sonnet 5, normal): tests/fixtures/uitest bundle rendered via scripts/render_uitest.py and driven live in a real browser (Overview, Network Inventory, Configuration, Compliance, Discovery, Exclusions, Project Plan all clicked); zero console errors/warnings across the session, all seven modules populated correctly with real fixture data. No code touched -- confirms the eight-file composed <script> executes identically to the pre-split monolith outside the bun/happy-dom and Playwright harnesses too.
