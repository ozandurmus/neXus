# Two verified feature_registry wording drifts contradict build_history and the shipped source

status: done · target: A DOCS movement in an engineering session; wording only, no criterion state or feature status change

Both drifts were flagged as [ASSISTANT] hypotheses in PRODUCT_DIRECTION_RECORD.md section 11 and are VERIFIED against source by the first Phase A PLAN episode (2026-09-08). Reported, deliberately not fixed here -- the PO assistant does not perform mechanical documentation work (nexus-po skill section 5). DRIFT A: project/feature_registry.json left_vertical_product_navigation.criteria[no_enrollment_affordance].label still ends 'The Administration -> Device Management second entry point (PO-NAV-1's lifecycle home) is not yet built -- deferred as a disclosed follow-up, not silently dropped.' That sentence is stale. project/build_history.json's M9 record states the entry point was built in round 2, and the source agrees: static/navigation_ui.js declares the device-management entry under the Administration root, static/console_actions.js implements consoleRefreshDeviceManagementTable() under the comment 'PO-NAV-1 -- Administration -> Device Management, the second frozen enrollment entry point', and templates/console.html ships the deviceManagementModule panel with a second open button bound to the one shared m9EnrollDialog. The entry point is console-shell-only by design (D-NAV6 shell integrity), which the navigation model's own comment states; the criterion state 'done' is correct and only the trailing sentence is wrong. DRIFT B: project/feature_registry.json failover_controlled_execution has status 'planned' and a summary ending 'implementation hard-gated, not started', while CURRENT_STATE.md and project/roadmap.json record OP.2.A/OP.2.B as IMPLEMENTED and the CP ClusterXL adapter, member session and preflight/eligibility provider as implemented, unit-tested and unwired. Source confirms checkpoint/clusterxl_capability_adapter.py, checkpoint/clusterxl_member_session.py, checkpoint/clusterxl_preflight_provider.py and utils/operate/{coordinator,authorization,eligibility}.py all exist. The five criteria states remain defensible -- nothing is reachable, no adapter is wired, no OPERATE RBAC role exists, and vendor_action_adapters covers three adapters of which one exists unwired -- so only the summary wording understates reality. Suggested replacement wording for the owning DOCS movement, to be reviewed rather than pasted: 'Vendor-independent architecture FROZEN 2026-09-04; several foundations implemented and unit-tested but structurally unreachable and unwired (no taxonomy member, unconditional DENY authorizer, no production entry point). Product execution not started.' NOTE FOR THE CONVERGENCE GATE: both drifts live in free-text label/summary fields, so project_metadata_has_no_cross_authority_contradictions cannot see them; extending that check to prose is a separate question, not part of this item. FIXED 2026-09-09 (feature/project-state-wording-drift-reconciliation): both drifts re-verified still accurate against current HEAD before editing. DRIFT A's trailing sentence corrected to state the entry point is built and console-shell-only by design (D-NAV6). DRIFT B's summary replaced with the suggested wording verbatim after confirming it still holds (utils/operate/__init__.py and utils/action_taxonomy.py confirm CLASS 2 remains memberless with DenyAllAuthorizer as the only wired Authorizer and no production entry point). No criterion/status state changed.
