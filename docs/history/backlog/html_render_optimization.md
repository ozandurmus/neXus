# Optimize HTML render wall-clock time based on the html_render_performance profiling findings

status: automated_validated · target: 0.6.x polish follow-up

AUTOMATED_VALIDATED 2026-08-31 (contract: docs/history/phase/PHASE0_6_X_HTML_RENDER_OPTIMIZATION.md). fill_template's ~40-46% cost turned out to be almost entirely json.dumps()/escape work over the embedded payloads, not the regex substitution itself -- resolved by splitting that JSON-building into its own build_json_replacements stage timer, which the re-measured profile confirms: build_json_replacements ~44-45%, fill_template itself only ~5.5-5.7% after the split. utils/html_export._fill_template's compiled sentinel pattern (previously re-sorted/re-escaped/re-compiled on every single render call) is now cached via @lru_cache(maxsize=32) keyed on the replacement-dict key tuple, removing that repeated work; _fill_template stays generic (still exercised with ad hoc key sets in tests/test_html_export_placeholder_integrity.py). No payload shape, sentinel set, or rendered-output byte content changed. build_compliance_posture (~31-34%, unchanged by this build) was investigated and deliberately NOT touched: its CE.1 regex-assertion path already carries a documented ReDoS timeout safety backstop (utils/compliance_check_engine.py), and its remaining per-subject x per-check cost is inherent evaluation work, not obviously wasteful -- restructuring it without real fleet-scale evidence (the fixture's 2 subjects is not representative) was judged too risky for a P2 item's speculative gain; stays a candidate for a future build gated on a real production-scale --render-only profile, same on_hardware_real_env_validation class gap as its parent item. Evidence: py -m pytest -q: 764 passed, 10 skipped, 2 failed (both pre-existing/unrelated test-order-pollution, confirmed pass in isolation) -- zero regressions. tests/test_html_render_harness.py: 5 passed, 1 skipped.
