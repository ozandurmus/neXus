# Per-HA-entity operational lock -- the CLASS 2 lock grain does not exist

status: done · target: OP.2.B (architecture FROZEN 2026-09-04 in docs/history/phase/OP_2_0_CONTROLLED_HA_OPERATION_ARCHITECTURE.md)

Closes the OP.0b.0 frozen contract's own bug/gap register row X-1 (docs/history/phase/OP_0B_0_VENDOR_FAILOVER_PREFLIGHT_EVIDENCE_SURFACE.md section 26: 'per-HA-entity lock (10.1 item 4) tracked nowhere ... P0 before CLASS 2 (record now)'), which had no backlog item, no roadmap row and no design section anywhere in the repository. FAILOVER_ENGINE_ARCHITECTURE.md section 10.1 item 4 requires a per-HA-entity lock held across preflight -> act -> verify. The lock that exists today is per PHYSICAL ENDPOINT -- utils.coordinator_backend.derive_lock_key(secret, 'endpoint', canonical_id), admitted by utils.collection_executor.CollectionCoordinator -- and two members of one cluster are two endpoints, so it is the wrong grain for an action whose subject is the cluster. MODEL (independent review 2026-09-04, docs/history/phase/OP_2_0_CONTROLLED_HA_OPERATION_ARCHITECTURE.md P8/P10): (1) the HA-entity lock IS the durable action record -- the store admits at most one record per operational_entity_id (CP ClusterXL group_id; CP VSX physical cluster parent; never a member, management address, SSH endpoint, API session or browser session) that is non-terminal or an unacknowledged OUTCOME_UNKNOWN, enforced by a create-race-safe uniqueness check; no new lock mechanism, key domain or table; (2) the existing per-endpoint coordinator admission (lease + vendor budget) is taken per device-contact stage -- the preflight, then precondition + submission + verification -- and NEVER across the human confirmation wait, because the lease is connection-lifetime (DEV.3.2) and the vendor budget (capacity 1) would otherwise block every collection of that vendor for an unbounded time; refused admission is ABORTED_PRE_MUTATION:member_busy, never a wait; (3) quarantine after OUTCOME_UNKNOWN is a derived predicate over the record store, lifted only by an authorized, audited acknowledgement -- a restart cannot lift it because reconciliation never rewrites a terminal state. Frozen by the product owner on 2026-09-04; implementation is OP.2.B's, a future movement behind OP.0b closure. CLOSED 2026-09-04: implemented in utils/operate/store.py (ActionRecordStore) exactly per the MODEL above -- see docs/history/phase/OP_2_A_B_EXECUTION_FOUNDATION.md.
