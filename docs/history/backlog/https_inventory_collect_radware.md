# Radware Collect: https_inventory_collect for the Cyber Controller (managed device list as inventory) and DefensePro (observed facts via the controller's list); DefensePro interfaces/routes/policies need new gated REST reads -- propose to the PO

status: planned · target: docs/design/RADWARE_CYBER_CONTROLLER_BACKUP_API_GATE_ENTRIES.md

2026-09-25 ~04:05: built for the Cyber Controller -- https_inventory_collect logs in, reads alldevices (gates radware_cc_login/alldevices/logout, already SIGNED_OFF) and records the managed device names as the controller's inventory run (names only; the list's field shape is logged as names/counts, MEASURE FIRST). A DefensePro appliance is still refused with VENDOR/ROLE rules? No: it is admitted on the same capability but the executor answers 'no HTTPS inventory read for vendor radware' -> the job fails honestly until the PO approves DefensePro reads. PROPOSAL for the PO: DefensePro REST reads worth gating for inventory -- interfaces/addresses (rsIfIndexTable or /mgmt/device/byip/<ip>/config/getcfg parsed as a configuration run), software/signature versions via /monitor?prop=, policies rsIDSNewRulesTable and profiles. Each needs a gate entry (action_class read) and a measured first run; none implemented.

2026-09-25 07:24: Cyber Controller Collect real-environment validated -- login, alldevices, logout in 381 ms, 4 managed devices recorded as the controller's inventory names. Measured alldevices field names: children, deleted, deviceVersion, formFactor, highAvailabilityPriorityEnum, managementIp, name, ormId, parentOrmId, status, supportTemplate, treeType, type. Next (same gate, no new read): a DefensePro appliance's Collect takes its version, form factor, status and HA priority from the controller's list entry that names its address.
