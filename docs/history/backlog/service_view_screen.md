# Service view: every neXus pod, its state, restarts, CPU and memory use (top-like), version, last rollout -- read-only, Administration; CLI counterpart

status: planned · target: PO 2026-09-22; needs a read-only metrics path from the service to the cluster (no kubectl in the browser)

2026-09-22 implemented in the combined fix; see the commit of the same day.
