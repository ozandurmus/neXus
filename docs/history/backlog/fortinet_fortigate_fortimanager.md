# Fortinet: FortiGate over SSH, FortiManager over JSON-RPC (V80)

status: automated_validated · target: 

Methods ready; PO adds the devices (fwadm). FortiGate: status, interfaces/routes per VDOM, backup = top-level show; paging answered in-session. FortiManager: JSON-RPC status + managed devices; its own backup next (SFTP push). docs/design/FORTINET_CONTRACT.md
