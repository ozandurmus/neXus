# uitest render-harness fixture never exercises a non-INSUFFICIENT_EVIDENCE failover verdict

status: done · target: OP.0c follow-up

DONE 2026-09-09: added tests/fixtures/uitest/cp_config_telemetry.json (both vsx-cls-01/vsx-cls-02 ACTIVE at once, over the existing vsx-cls VSX cluster already present in unified.json) and wired it through scripts/render_uitest.py's run_html_export(checkpoint_config_result=...) call. This exercises the real build_failover_readiness_payload/compute_ha_readiness split-brain path (no change to verdict-computation logic) and now renders unit_id 'vsx-cls' as UNSAFE_DO_NOT_FAILOVER / split_brain_observed. Proof: tests/test_html_render_harness.py::test_failover_readiness_data_renders_a_genuine_unsafe_row asserts the rendered failoverReadinessData payload's verdict/reason/summary directly, not just fixture-file validity. (Note: cp-core-01/cp-core-02 in unified.json cannot be used for this -- cp-core-01 carries a cluster_topology.group_id that diverges from cp-core-02's legacy 'cluster' field, so they resolve to two separate 1-member units instead of one 2-member cluster; that is a pre-existing fixture quirk, left untouched here since fixing it is out of this movement's scope.)
