# Bootable service: Spring entry point, DataSource, Flyway at startup, empty UI shell with menus

status: planned · target: Phase 1 step 1; no device contact

2026-09-13 realignment: closed as delivered. The service composition root exists, Flyway runs at startup, the shell is routed behind an AuthGate and login is required even on localhost.
