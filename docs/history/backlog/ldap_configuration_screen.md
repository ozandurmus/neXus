# Implement Administration LDAP connection and trust settings screen

status: planned · target: PAUSED by user; screen/API not implemented; backend and login selector only

The LDAP configuration menu and its settings API are NOT IMPLEMENTED. Existing delivery contains C3C adapters, typed principal/group bindings, server-owned environment configuration and local/ldap login selector. No user proof of running LDAP-profile helper, LDAPS/STARTTLS handshake, ordinary-user directory login or AD identity-control support. User cancelled work before screen implementation; preserve this open gap.
