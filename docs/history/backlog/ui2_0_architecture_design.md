# UI 2.0 -- architecture design: separate Java operator console, LDAP multi-admin sessions, profile-based backup, per-feature port from Line-1

status: done · target: docs/design/UI2_0_ARCHITECTURE_DESIGN.md -- DRAFT, design resolved, awaiting Product Owner review, external second opinion and a formal DECIDE/freeze episode (relay/NXS-LOCAL-0047, 2026-09-09). Not implementation authority; freezing it authorizes no dispatch by itself. DONE: PR #160 merged 2026-09-09; superseded in part by UI2_0_DEVELOPMENT_WORKFLOW.md (BASELINE rev 2) and UI2_0_BASELINE_CONTRACT.md (FROZEN).

Successor to ui2_0_architecture_requirements. RESOLVED as stated decisions per the Product Owner's 2026-09-09 direction: C-1 -> UI 2.0 is a separate shell/application (CON.0 section 6 payload parity replaced by 'both surfaces read the same persisted projections'; CON.0 section 3 bundler rule narrowed to the shared report bundle; replacement text drafted as UA-1/UA-2 in section 4.3 plus a one-sentence PCP section 13 companion). C-2 -> RBAC is D1/D7, never menu-hiding: action_id -> role token (repo) -> role binding (encrypted PostgreSQL row, security_admin-edited, audited) -> AD group reference; evaluated per request in the Java gate chain E1..E7 (section 5). C-3 -> Backbox-style profile-based backup subsystem: onboarding stays PCP.1/M9; a profile is a versioned, validated, four-eyes-approved ordered step sequence (connect/exec/poll/sftp_get/verify/disconnect) each with an expectation gate, expect-script semantics stated testably, raw transcript discarded (shape + fingerprint only); browser does select/assign/schedule/approve/read; 'back up now' from the browser refused always (class 1 stays RB.x + scheduler/CLI); a D7-authorized audited schedule-edit intent succeeds C-D7 with server-owned floors/ledger/aggregate-contact ceilings (section 6). C-4 -> concurrent multi-admin, single active session per identity (takeover|refuse at login), LDAP instead of OIDC as DEPLOY.1A's successor; M14 decisions carried over or amended one by one (LD-2/LD-4/LD-6 unchanged; LD-1 + a read-only re-validation service account; LD-3 sessions/group state persisted encrypted; LD-7 replaced by per-login browser bind over TLS; LD-5 re-asked -> UnboundID) (section 7). U-1 -> full Java stack, ported per feature via the F0..F5 ladder (F2 projected, F3 intent-routed, F4 shadow with parity + real-env MATCH, F5 authoritative); Spring Boot 3 / jOOQ + Flyway / UnboundID / Apache MINA SSHD / Gradle / container image recommended with tradeoffs; CE.2 command-primitive registry ported as the worked example with sealed-interface + compact-constructor + ArchUnit enforcement and the 33 Python tests exported as the Java fixture table (section 8). X-2 -> recorded: PostgreSQL production engine, SQLite local, CAS/recovery on the volume, Oracle deferred-not-rejected with cost-free portability rules (section 9). STILL OPEN (section 11/12): eight contradictions with frozen/constitutional authority reported for the freeze decision (UA-1..UA-8, notably AGENTS.md 'no browser-originated command' vs a Phase-2 in-browser profile editor -- Phase 1 file/CLI authoring needs no amendment); M14 U-1 nested-group semantics; DEV.4.6 and off-host custody gaps from PCP_STORAGE_ENGINE_DECISION.md; corporate JDK/build-tool confirmation; Backbox screenshot items (defaults chosen, none blocking); repository placement of the Java tree (recommendation: this repository). Next: PO review -> external second opinion -> DECIDE episode on the document plus UA-1..UA-4; first engineering movement if frozen: a CONTRACT draft for F2 on two features (registry rows, HA readiness projection) plus the UI 2.0 skeleton, Sonnet 5 extended (high).
