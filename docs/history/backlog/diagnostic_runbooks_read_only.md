# Diagnostic Runbooks (CLASS 0, closed catalog, shared primitive registry with CE.2)

status: automated_validated · target: PCP.8

docs/design/PRODUCT_CONTROL_PLANE_ARCHITECTURE.md section 15: closed source catalog of bounded ordered read-only sequences over registered targets; every step gate-reviewed for this purpose; timeout/runtime, output limits, deterministic framing, redaction, audit; validator rejects class 1+ steps. Never browser -> arbitrary shell; no user-authored scripts in the first implementation. The CE.2 curated primitive registry and the runbook step catalog are one registry. IMPLEMENTED 2026-09-09 (relay/NXS-LOCAL-0045): new configuration/runbook_catalog.py -- a Runbook dataclass (runbook_id, vendor, title, purpose, ordered step_ids referencing configuration.command_primitives.PRIMITIVE_REGISTRY entries only) plus register_runbook()/validate_runbook_steps(), the runbook-layer choke point every step passes through: fail-closed if a step_id is not already a registered primitive, if its action_class_id is not utils.action_taxonomy.CLASS_0_READ, or if its vendor does not match the runbook's declared vendor -- independent defense-in-depth atop command_primitives.register_primitive's own gate, proven against a hostile registry object in tests, not merely re-reading the same already-trusted check. RUNBOOK_CATALOG holds one concrete example (AC-4): cp_basic_health_check, a single-step Check Point runbook reusing CE.2's own cp_gaia_show_version_all proof primitive. No second registry: every resolved step is the literal PRIMITIVE_REGISTRY object (identity-checked in tests), execution reuses configuration.command_primitives.run_check_point_primitives/run_palo_alto_primitives unchanged via their existing primitives= extension point (CE.2's own stated seam) -- no new transport, no new credential path. New application/workflows/runbook.py wires main.py --runbook-execute --runbook-id <id> (application/cli.py), modelled on --compliance-probe: reuses its exact target-selection helpers (no second target-selection policy for the same evidence question), the existing utils.collection_executor admission coordinator, writes a new local-only redacted evidence artifact (output/runbook_execution_<runbook_id>_<timestamp>.json, sensitivity LOCAL_OPERATOR_SENSITIVE_NOT_SHAREABLE), and is mutually exclusive with every other collection/render/maintenance mode; not registered in console/registry.py's JOB_REGISTRY (never console/browser-submittable). Deterministic framing, output limits and redaction are CE.2's own mechanism reused unchanged -- every step's PrimitiveResult.redact() still discards raw output in the function that captured it; no new redaction rule was written. Evidence: 33 new tests (tests/test_pcp8_diagnostic_runbooks.py, tests/test_pcp8_diagnostic_runbooks_cli.py) covering validator rejection (unregistered step, a fabricated CLASS_1 step, vendor mismatch, empty sequence), shared-registry identity, step-order preservation, CLI parser/mutual-exclusion/not-console-submittable, and an end-to-end mocked-transport run proving raw command output never survives into the written artifact. Status held at automated_validated rather than done, matching compliance_check_engine_primitives' own precedent immediately above it in this file: this catalog/validator layer is itself fully exercised by automated tests, but it executes entirely through CE.2's run_check_point_primitives/run_palo_alto_primitives, whose own real-device output-shape confirmation is still owed (no live device reachable from this workspace) -- promoting this entry to done would claim real-environment evidence this movement does not have, contradicting AGENTS.md 'Never mark a network-facing behavior DONE from automated tests alone.' Tracked on the same real-environment-validation-owed line as CE.2 in CURRENT_STATE.md.
