# M8.3 first-contact identity-evidence producer: real-environment validation deferred by explicit Product Owner decision

status: real_env_validated · target: M8.3 (utils/first_contact_producer.py, --identity-first-contact); gated M8.4's own §9 requirement for M7 -- now paid

RESOLVED 2026-09-08: the Product-Owner-authorized bounded read-only command ran for real against a Product-Owner-named registry device_id (device_id 65aca68e9bd54ebc8fea9fd8face81db, endpoint <PRIVATE_IP> -- a real Check Point ClusterXL member, <DEVICE_NAME>, discovered via the same session's own `--only cp` run against MDS <PRIVATE_IP>), executed by the Product Owner directly on their Windows machine per AI_START_HERE.md's Real-environment procedure (agent proposed the exact bounded command; human performed the actual network/credential contact -- credentials never entered the agent's context, via getpass interactive prompt). First attempt returned `endpoint_not_trusted` because the physical host's SSH host key was not yet in the operator's `~/.ssh/known_hosts` (the --only cp bulk collector's AutoAddPolicy compatibility mode does not populate the strict trust-before-credential known_hosts source M8.3 requires) -- resolved by one manual `ssh <user>@<PRIVATE_IP>` login to populate known_hosts, confirmed present (plain ssh-ed25519 entry, unhashed) before retry. Second run: `outcome: NEW`, `relationship_id: e4671bc45e5e49e2bf9257d32b3fa067` -- a proven device_id -> collector entity_id relationship, the full trust -> credential -> RejectPolicy -> identity gate -> serial -> governed evidence -> relationship-write sequence exercised for real. M8.3 is now genuinely REAL_ENV_VALIDATED per the frozen M8 contract section 9 -- no automated fixture or synthetic relationship satisfies this gate, only this real run does. M7 ('real device-targeted Collect now') is UNBLOCKED to start (its own implementation has not begun -- unblocking is not the same as M7 being done). The CP config evidence-retention lower bound/window measurement (frozen contract section 6/section 9) was not separately extracted from this run's evidence in this session -- if still needed, a small follow-up can read it from the relationship's producing_run_ref rather than requiring a second real-device contact.
