# Classify collectable unknown CP platforms without blocking current-state evidence

status: automated_validated · target: 0.6.1C

AUTOMATED_VALIDATED 2026-08-30. Contract: docs/history/phase/PHASE0_6_1C_CP_UNKNOWN_PLATFORM_LIFECYCLE.md. CapabilityProfile.platform_family/.platform_confidence added, independent of every other field (proven: plan_collection() output is identical across every platform_family value); platform_fields_from_classification() is the one sanctioned conversion from a real _classify_platform() dict, integration-tested against the actual collector function. discovery_capability_ui.py + app.js's Discovery module surface it (new Platform column). Scope correction discovered during implementation: there is no live 0.6.1C Phase 4 collector-to-CapabilityStore wiring anywhere in the repo yet for ANY capability field (not platform-specific) -- building that is materially larger and stays a separate future item, not silently absorbed here. Also flagged (not fixed): tests/fixtures/uitest/discovery_ui.json's entity keys pre-existingly do not match _entity_row()'s real shape -- see new backlog item discovery_fixture_shape_drift. +11 regression tests; py -m pytest -q: 569 passed, 2 skipped, 2 failed (both pre-existing/unrelated) -- net +14, zero regressions.
