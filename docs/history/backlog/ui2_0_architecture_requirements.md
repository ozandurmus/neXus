# UI 2.0 -- empty-shell, DB-backed, LDAP-authenticated, role-scoped live operator console: requirements-and-tradeoffs draft

status: done · target: docs/design/UI2_0_ARCHITECTURE_REQUIREMENTS.md -- DRAFT, requirements and tradeoffs only (relay/NXS-LOCAL-0025, 2026-09-09). SUCCEEDED 2026-09-09 by docs/design/UI2_0_ARCHITECTURE_DESIGN.md (backlog id ui2_0_architecture_design, relay/NXS-LOCAL-0047), which resolves every open point this draft left (C-1..C-4, U-1; X-2 recorded). This draft stays the evidentiary base (its section 2 audit and section 7 sketch); it is not a scope decision and not implementation authority. DONE: PR #140 merged 2026-09-08.

Code-grounded audit (section 2) shows the static-export coupling is narrow and located: six run_html_export tail calls in application/workflows/* plus the eight *_ui.py payload builders as the console's only read model; the registry, M4 control-plane store, seven Postgres-capable evidence concerns, the admission coordinator and the D4/M10.2 resolver chain are already DB-ready-shaped. Recommendations (section 6, not decisions): incremental DB + typed live-state API over the existing foundation rather than a rewrite; PostgreSQL as the production engine against PCP section 10's own criteria (SQLite stays local, CAS/recovery blobs stay on the volume); collection modules stay Python workers behind a queue-through-database boundary; Java, if binding, only for the new service/API/DB layer (option i), never a Python-to-Java rewrite of the real-env-validated collectors (option ii). Reports six contradictions with frozen authority (C-1..C-6: CON.0 payload parity and bundler rule, AC-CS-33 menu hiding, class 1 backup from a browser, M14 single-operator vs multi-user login, HA cadence vs polling gate, production write exposure) and six unknowns (U-1 the exact Java requirement first). Next step is a Product Owner DECIDE episode, not an engineering movement.
