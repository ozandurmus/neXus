# HOST-A rebuilt 2026-09-25: PO browser acceptance under aiview -- pseudonyms unchanged, CP+PAN collect decrypts credentials, an old backup opens, Cyber Controller backup completes

status: in_progress · target: docs/design/HOST_A_REBUILD_RUNBOOK.md section 5

2026-09-25: recorded in build NXS-LOCAL-0369; host-side checks done (site 200, schema 70, 110 devices, 1,515 store files); PO browser checks pending.

2026-09-25 (aiview, Chrome extension + host CLI): pseudonyms unchanged (five key Secrets byte-identical to the export); Check Point and Palo Alto Collect COMPLETED (18 s / 5 s); an old backup's Contents opens (13,292 members). Cyber Controller Backup Now FAILED: the controller's OpenSSH/lftp client pins HOST-A's pre-reinstall host key and, on the mismatch, disables password authentication (sshd DEBUG3: next methods publickey,password, then the client closes; no password attempt). Not a receiver/password problem. Fix is on the Cyber Controller (stale known_hosts entry) or an alternate spelling of the receiver address; the 03:00 controller backup fails the same way until then. A manual export by the PO left backup nexus-4ae0578d8cd2c6c4 on the controller (delete step timed out). Side findings: ui2-service OOM-killed at its 2Gi limit opening a device page, limits raised (service/worker 8Gi/4 CPU, compliance/configuration 2Gi/1 CPU); aiview granted role:backup_admin and role:compliance_admin (PO: aiview's boundary is masking, not admin actions; security_admin withheld).
