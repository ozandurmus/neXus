# Manual device add: IP + vendor + transport method (SSH/API/SNMP) selection

status: planned · target: Phase 1 step 3; transport use is gated

2026-09-13 realignment: closed as a duplicate. B1-04B already specifies the manual registration dialog (name, vendor, transport, credential reference) and 13C section 3 fixes Individual Device as a type within the one device-add entry point.
