# CI's 'Repository privacy gate' step fails unconditionally on every PR -- needs the same baseline-scoping fix already shipped for the local engineer hook

status: done · target: Merged PR #135, feature/gov-po-3-ci-privacy-gate-baseline-scoping. Follow-up to relay/NXS-LOCAL-0014 (GOV_PO_3_PUSH_HOOK_BASELINE_SCOPING, merged PR #127).

Found live 2026-09-08 while the Product Owner was monitoring five in-flight orchestrated movements: the GitHub Actions 'validation' workflow's 'Repository privacy gate' step (python main.py --repository-privacy-check, .github/workflows/validation.yml lines ~92 and ~172) scans the full working tree and fails unconditionally on the same two pre-existing, already-accepted, build-history-immutable false positives (project/build_history.json CREDENTIAL_LITERAL, relay/NXS-LOCAL-0003-local-relay-watch-command.json CREDENTIAL_LITERAL) -- confirmed failing identically on PR #127, #128, #129, #130, #131. relay/NXS-LOCAL-0014 already solved this exact problem shape for the LOCAL engineer pre-push hook (scripts/nexus_engineer_tool_gate.py, content-fingerprint baseline matching, not line-number matching) but that fix was never applied to the CI-level invocation of the same underlying scanner (application/cli.py's --repository-privacy-check path). There is no branch protection on this repo (confirmed via gh api repos/.../branches/main/protection -> 404 'Branch not protected'), so this has not blocked any merge so far -- engineers have been self-merging under the standing relay#13 authorization regardless of the red CI badge. Left unfixed, CI shows a permanent, uninformative red 'validate' status on every future PR, which defeats CI's own purpose: a genuinely new leak would look identical to this known noise. Fix direction (a full SESSION_START packet was already drafted 2026-09-08 at /private/tmp/nexus_po_ci_privacy_gate_packet.json before the Product Owner asked to hold dispatch for a new chat session): extract relay/NXS-LOCAL-0014's baseline-comparison logic into a shared module (utils/repository_privacy.py is the natural home) used by both the local hook and the CI entry point; CI's baseline is the PR's merge-base with origin/main (verify what ref info is actually available in this repo's GitHub Actions checkout for a pull_request event, and whether .github/workflows/validation.yml's checkout step needs a fetch-depth change to compute it); a finding present in both baseline and current scan is pre-existing debt and must not fail the gate, a finding only in the current scan still fails exactly as today. This movement's own PR will likely hit the same currently-broken CI check before its fix lands -- expected and self-resolving, same precedent as relay/NXS-LOCAL-0014's own SESSION_START noted.
