# A third, previously-uncatalogued prose collision in relay/NXS-LOCAL-0012-gov-po-3-ac5-demo-1-scratch.json blocks the same two DLP collision-assertion tests, independent of the two already-decided findings

status: done · target: Governance decision needed, same shape as dlp_scanner_prose_collision_in_frozen_records; discovered while implementing that item's fix

Discovered 2026-09-09 (NXS-LOCAL-0042) while implementing the narrow, hash-keyed exception for dlp_scanner_prose_collision_in_frozen_records's two named findings: relay/NXS-LOCAL-0012-gov-po-3-ac5-demo-1-scratch.json line 94 (its own recorded RELAY_DECISION text, quoting 'the literal substrings 'pass'+'word'=' and 'PASS'+'WORD:'' verbatim while explaining why they are false positives) itself contains both collision token shapes, and is not one of the two findings this movement was authorized to except. This file was committed to main in commit a911ed5 ('gov_po_3_session_bookkeeping: sync missing relay files to main...', 2026-09-08 21:13:59), syncing relay files for already-merged movements that had never actually been committed -- i.e. it only became a tracked-file candidate for these scanners after that sync, which likely postdates whatever scan first established '2 failed' as the known-failure count. Same three-option shape as dlp_scanner_prose_collision_in_frozen_records applies here (standing waiver / narrow exception / prose redaction), and the same build-history-immutability question may or may not apply -- unlike build_history.json and the M9 relay file, this is a RELAY_DECISION record rather than a build-history entry, so the Product Owner may judge it differently; not decided here. Filed rather than decided, matching this item's own precedent's AC-6 reasoning; not added to NXS-LOCAL-0042's own narrow allowlist, which stays scoped to exactly the two originally-named findings. RESOLVED 2026-09-09 (NXS-LOCAL-0044), PO decision: treat identically to the two originally-catalogued findings -- option (b), narrow allowlist entry. Re-verified line 94 of the relay scratch file is still the exact colliding line (unchanged since NXS-LOCAL-0042's diagnosis), computed its sha256, and added it as a third entry to _KNOWN_PROSE_COLLISION_LINE_HASHES in tests/test_dev_0_5b_auth_consumer_canonical_config.py -- no change to the matching mechanism itself. Full one-shot regression (`/Users/OzanDur/Codo/neXus/.venv/bin/python -m pytest -q -n auto --dist worksteal`): 3131 passed, 27 skipped, 0 failures -- the literal 0-failure result this item required.
