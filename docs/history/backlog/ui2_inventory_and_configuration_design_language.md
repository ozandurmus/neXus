# Apply the inventory and configuration design language: cluster as the unit with virtual systems beneath it, agreement rendered once and difference rendered explicitly, stated identity, freshness, coverage counters and provenance

status: planned · target: docs/design/UI2_INVENTORY_AND_CONFIGURATION_DESIGN_LANGUAGE.md (DRAFT, needs freeze with D-UI1 and D-UI2 answered)

2026-09-22 (commit 8f96a53): Configuration detail stripped of every mockup remnant (hardcoded alignment table, Aligned/drift pills, Panorama/Policy header copy, dead Export/Compare buttons, hostname-based Override filter) and rebuilt on device evidence only: shared InventoryEntityHeader, 'Collected <time> · Primary source: <read_kind>', withheld-line statement, real section index, real V22 deviation summary (Changes tab), overrides, sanitized text; Alignment states expected-vs-actual is not collected in this build. Inventory: standalone device and cluster now share one header and one interface/route table family (commits 30dbab8, 712691f, 4614ce3). Still open against the FROZEN doc: Configuration list is still one row per device (§2 wants the cluster as the unit with member differences per §3; needs cluster_member_ref on GET /configuration) and the §7 collapsible identity summary tiles.

2026-09-22: Configuration list now presents the cluster as the unit (section 2): members grouped under their cluster reference with per-member change-state chips and projected-setting counts; agreement judged by canonical hash (Members agree / Config diff / UNKNOWN when a member was never collected). GET /configuration carries cluster_member_ref, canonical_hash, projected_settings. Still open: section 7 collapsible identity summary tiles on the configuration detail.

2026-09-22 (PO: 'Config kodları perişan; bu ekran gibi olmalı; küme görünümü; iki üyede fark varsa DIFF; değerler gizli olmasın'): Configuration rebuilt on the old product's shape -- inventory tree on the left (fleet/vendor chips, cluster -> virtual systems), device detail with entity header, 'CURRENT ACTUAL · N projected settings · source plane · withheld', operator snapshot tiles, per-section SETTING/CURRENT VALUE/ORIGIN/CONTEXT tables (System, DNS, NTP, Management, Password Policy, Login Banner, Logging, HA, Interfaces, Routing, SNMP, AAA, Users, Other), filter box; cluster detail shows members side by side with DIFF rows (member-specific settings marked MEMBER, not counted), 'differences only' switch. Projection is a pure client-side parser over the sanitized text (configurationProjection.ts, tested). Old detail kept under Details tab.

2026-09-22 PO second pass: no VS/VSX nodes in the Configuration tree (both vendors); the entity's own detail lists them. Added a Platform identity card (vendor, model, software version, hotfix/jumbo, serial, HA role, virtual systems, management address, enrollment, last read) on device and cluster views; hotfix/jumbo and serial show UNKNOWN 'not collected' -- they need their own gated reads (CP: show asset / installed jumbo take; PAN: serial is read as identity but not exposed) -- PO decision needed on exposing serials. PAN cluster/device views were empty because the text-available flag looked only at the primary (effective-running) run; fixed to consider the active read's text.
