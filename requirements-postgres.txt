# Optional — only needed when SECURITYEXPERT_COORDINATOR_BACKEND=postgres
# (DEV.3.2 distributed per-endpoint lock; see
# docs/history/phase/DEV3_2_DISTRIBUTED_ENDPOINT_LOCK.md). The default
# in-memory coordinator backend needs none of this. Ships as a manylinux
# wheel (binary extra), so no build toolchain is required — consistent with
# the Dockerfile's no-build-toolchain constraint (DEV.3.1).
psycopg[binary]>=3.1
