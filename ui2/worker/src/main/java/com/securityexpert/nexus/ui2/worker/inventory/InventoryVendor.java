package com.securityexpert.nexus.ui2.worker.inventory;

/** The two vendors {@code inventory_collect} runs against -- no third value, mirrors {@code worker.confirm.Vendor}. */
public enum InventoryVendor {
    CHECK_POINT,
    PALO_ALTO
}
