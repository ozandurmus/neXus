apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base/
  - postgres-test.yaml

images:
  - name: nexus-ui2
    newName: image-registry.openshift-image-registry.svc:5000/default/nexus-ui2
    newTag: latest
