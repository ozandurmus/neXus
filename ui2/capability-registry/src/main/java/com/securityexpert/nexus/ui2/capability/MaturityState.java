package com.securityexpert.nexus.ui2.capability;

/** {@code capability_registry.maturity_state} (C4 §2.2, baseline §1's ladder). */
public enum MaturityState {
    CAP_SPEC,
    CAP_OFFLINE,
    CAP_VALIDATED,
    CAP_RELEASED
}
